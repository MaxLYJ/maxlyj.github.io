# Code Review — maxlyj.github.io

**Reviewer:** Technical Director review pass
**Date:** 2026-06-30
**Scope:** Homepage (`index.html`, `home.js`, `style.css`), project-instance pages (`*.html` + `project-instance-loader.js`), shared template (`template-content.html`), data (`data/taxonomy.json`), SEO/config files.

Priority order used throughout: **critical bugs → accessibility → performance → content polish → architecture**.

---

## ✅ Fixed in this iteration (iteration 1)

### Critical / hygiene
- **Deleted a junk file committed to repo root** — `udo apt install openssh-server` was an accidental shell redirect containing a `git log` dump. It was tracked by git and shipped to production. Removed via `git rm`.
- **Removed leftover debug `console.log`** in `home.js` (sidebar click handler). It logged `scroll-margin-top` / `scroll-padding-top` on every nav click — a debugging artifact from commit `30b2f9d`. Also removed the three now-dead local variables (`toPx`, `targetMargin`, `rootPadding`) it depended on, with a clarifying comment that the offset is now handled purely by CSS.

### Accessibility (a11y)
- **Removed invalid ARIA `role="note"`** on the tag-system status banner in `index.html`. `role="note"` has inconsistent AT support; the element is informational only.
- **Added a "Skip to content" link** as the first focusable element of `index.html` (`<a class="skip-link" href="#main">`), plus `id="main"` on `<main>`. Essential for keyboard / screen-reader users to bypass the sidebar/header chrome. Styled to appear only on focus.
- **Added a global `:focus-visible` focus ring** (`outline: 2px solid var(--accent)`) so *all* interactive elements (sidebar nav links, social icons, brand link) get a visible keyboard-focus indicator. Previously only `.tag-chip`, `.fr-thumb`, `.fr-nav`, and `.project-thumb` had explicit focus styles — links and icon buttons were focus-invisible.

### Performance
- **Reserved portrait image space** to prevent layout shift: added `width="185" height="185"` (intrinsic size), `decoding="async"`, and `fetchpriority="high"` to the About-section portrait (`index.html`). `.about img` had no dimensions or `aspect-ratio`, so it caused CLS on load.
- **Lazy-loaded off-screen gallery imagery**: `loading="lazy"` + `decoding="async"` on every card image generated by `createWorkCard` (`home.js`) and `renderRelatedWorks` (`project-instance-loader.js`).
- **Deferred featured-image decode** (`decoding="async"` on `#fr-main-image`).

### Content / SEO
- **Fixed the featured-card pre-hydration mismatch** in `index.html`. The static fallback showed cover art for *Far Cry 6* but the title/description read *“Division 2 / Tom Clancy's The Division 2 featured recommendation set.”* — wrong and confusing for no-JS users, crawlers, and the brief flash before `home.js` hydrates. Aligned title/description/alt to Far Cry 6 (the first carousel entry). Also replaced the generic alt *“Featured project cover”* with a descriptive one.
- **Standardized the X/Twitter URL**: the footer used `https://twitter.com/MaXLYJ` while the sidebar and template used `https://x.com/MaXLYJ`. Unified on `x.com`.
- **Corrected brand spelling “Farcry 6” → “Far Cry 6”** (Ubisoft's official wordmark) in `data/taxonomy.json` (card title + alt) and `farcry-6.html` `<title>`. The About section already used the correct form, so this removes an internal inconsistency.
- **Added full SEO metadata to all four project instance pages** (`farcry-6.html`, `division-2.html`, `d-walker-vs-sahelanthropus.html`, `raiden-vs-gekko.html`): `<meta name="description">`, `<link rel="canonical">`, Open Graph (`og:title/description/url/type/image`), and Twitter card tags. These pages were previously bare shells with only a `<title>` — invisible to search and social previews.
- **Loaded the Inter web font on project instance pages.** They referenced `style.css` (which sets `font-family: "Inter", …`) but never linked the Google Fonts stylesheet, so they silently fell back to `system-ui` — a visible inconsistency vs. the homepage. (Root cause: `project-instance-loader.js` injects only `<body>` markup from `template-content.html`, never its `<head>`.) Added `preconnect` + the Inter stylesheet link to each page.

---

## ✅ Fixed in iteration 2 (homepage hero: perf + accessibility)

### Performance
- **Memoized the taxonomy fetch in `home.js` (P1 #5).** The manifest was fetched redundantly on every homepage load: once at module scope for the tag system and again inside the featured-carousel IIFE via a separate `loadFeaturedTaxonomy()` helper — neither was memoized. Refactored `loadTaxonomyManifest()` to memoize a single in-flight promise (mirrors the pattern already used in `project-instance-loader.js`), return the parsed manifest, and apply it to the module globals via a new `applyTaxonomyManifest()`. Deleted `loadFeaturedTaxonomy()`; the carousel IIFE now consumes the same memoized manifest. Net effect: **one `data/taxonomy.json` fetch per page load instead of 2–3.** Also hardened `buildFeaturedPages()` against a `null` manifest (was previously guaranteed a `{ projects: [] }` object).

### Accessibility (carousel as an ARIA widget — P2 #6)
- **The featured-recommendations block is now announced as a carousel to AT.** Added `role="region"` + `aria-roledescription="carousel"` and a descriptive `aria-label` ("Recommendation carousel", kept in sync with the visible label) to the `<section>`.
- **Slide changes are announced.** Added a visually-hidden polite live region (`#fr-live`, `aria-live="polite"` `aria-atomic="true"`) updated on every `renderPage()` with `"Slide N of M: <title>. <description>"`, so advancing the carousel is spoken to screen-reader users.
- **Arrow-key navigation.** Added a `keydown` handler on the carousel section so Left/Right arrows advance the slide whenever focus is anywhere inside the carousel (prev/next buttons, thumbnails, or the slide link). `preventDefault` stops horizontal page scroll.
- **Control relationships.** Added `aria-controls="fr-card-link"` to both prev/next buttons so AT associates them with the slide they update.
- **Decorative indicator hidden from AT.** The dot indicator (`#fr-page-indicator`) is purely visual now that position is announced via the live region; marked it `aria-hidden="true"` and removed the contradictory `aria-label` the JS previously set on it.
- **Design note — intentionally kept the card as a link.** The APG carousel pattern suggests `role="group" aria-roledescription="slide"` on each slide, but the slide here *is* the primary call-to-action (an `<a>` to the project page). Overriding its implicit `link` role would have hidden the navigate affordance from AT — a worse regression than the "slide" affordance is worth. The card stays a plain link whose accessible name comes from its content; carousel semantics are carried by the region + live region + arrow keys instead.
- **Bonus correctness:** the static fallback `href` on the card was `template-content.html` (the raw shared template — a dead link for no-JS users / crawlers / the pre-hydration flash). Corrected to `farcry-6.html`, the first carousel entry's real target (matches the static Far Cry 6 fallback content and the taxonomy `url`).

### Validation
- `node --check home.js`, `node --check project-instance-loader.js` — pass.
- `data/taxonomy.json` — valid JSON.
- Served locally (`python3 -m http.server`); `/`, `/home.js`, `/data/taxonomy.json`, `/farcry-6.html` all return 200; the carousel ARIA attributes render in the served HTML.

---

## ✅ Fixed in iteration 3 (project pages: robustness + repo hygiene)

### Correctness / robustness
- **Visible load-error state on project pages (P1 #2).** `loadProjectInstanceTemplate()` previously `return`ed silently on every failure path, leaving a blank page with only the `<noscript>` hint (which is invisible to JS-enabled visitors). Added a `renderProjectLoadError(root, detail)` helper that injects a styled, `role="alert"` block into the mount point — announced to screen readers on insertion — with a short explanation and a "Return to homepage" CTA. It now fires on **four** distinct failures, each with a tailored message: (a) missing `data-project-slug` on the host page, (b) the per-project config fetch returning null (404 / network / invalid JSON), (c) the shared `template-content.html` fetch throwing or returning non-OK, and (d) the template parsing to a DOM missing one of `header`/`overlay`/`sidebar`/`main` (malformed template). The `!root` case still returns (nothing to render into — a misconfigured host page). The previous unguarded `fetch("template-content.html")` could also throw on a network failure with no try/catch; that call is now wrapped so it degrades to the error state instead of an unhandled rejection.
- **Removed the `project-instance-test` artifact from the live site (P1 #3).** `git rm`'d `project-instance-test.html` and `Resources/Project Instances/config/project-instance-test.json`. The page had `data-project-slug="project-instance-test"`, was absent from `sitemap.xml`, and was reachable by anyone; it now 404s. The four remaining project pages were verified to still map `data-project-slug` → an existing config. (Caveat: the matching `projects/project-instance-test/` folder may still physically exist in the R2 `portfolio-images` bucket — it's now an unreachable orphan and harmless; `IMAGE_STORAGE_PLAN.md` documents R2 provisioning state and was intentionally left unchanged since the bucket wasn't touched.)

### Validation
- `node --check project-instance-loader.js`, `node --check home.js` — pass; all five remaining config JSONs + `data/taxonomy.json` — valid.
- Served locally (`python3 -m http.server 8765`): `/project-instance-test.html` → **404**, `/Resources/Project Instances/config/project-instance-test.json` → **404**, `/farcry-6.html` → **200**, `/project-instance-loader.js` → **200**; the served `style.css` includes the `.project-load-error` rules and the loader includes `renderProjectLoadError`.

---

## ✅ Fixed in iteration 4 (repo hygiene: untrack the Wix archive)

### Architecture / maintainability
- **Untracked the 13 MB `Resources/Wix/` saved-site archive (P5 #21).** The directory shipped 72 tracked files (an entire saved Wix site: minified React/thunderbolt bundles, `*.js.download` / `*.css.download` artifacts, YouTube embeds, a screenshot) — of which exactly **one** file was referenced by the live site: the brand logo `Liu_WP_Logo_4x.png` (3.8 KB, 119×60 RGBA). The rest was brand-confusing bloat that inflated the repo and the deployed Pages checkout for no benefit.
- **Relocated the logo to a clean path.** Copied `Resources/Wix/HOME _ Yujia Max Liu_files/Liu_WP_Logo_4x.png` → **`images/logo.png`** (the existing `images/` directory already serves the portrait), verified byte-for-byte identical with `cmp`, and repointed both references — `index.html:58` and `template-content.html:23` — to `images/logo.png`. Git recorded the move as a **rename** (`R`), so logo provenance/history is preserved even though the path is clean. Chose a local-repo path rather than the R2 CDN because the logo is served locally (relative `src`) and the R2 bucket's per-object state is documented as unprovisioned (`IMAGE_STORAGE_PLAN.md`) and not verifiable from the repo — relocating locally achieves the goal without depending on unverifiable bucket state.
- **Prevented re-introduction** by adding a scoped `Resources/Wix/` rule to `.gitignore` (with an explanatory comment). The rule is deliberately scoped to `Wix/` only — the sibling `Resources/` subdirectories (`Project Instances/config`, `Project Template/pages`, `Featured Recommendations/pages`, `SideBar`) ARE live and fetched/referenced by `home.js` / `project-instance-loader.js` / project configs, so ignoring all of `Resources/` would silently break the site.

### Validation
- `node --check home.js`, `node --check project-instance-loader.js` — pass.
- `git diff --cached --diff-filter=D` — 71 deletions, **all** under `Resources/Wix/` (nothing unexpected removed); the 72nd Wix file is the renamed logo (`R` → `images/logo.png`).
- Served locally (`python3 -m http.server 8766`): `/images/logo.png` → **200** (3890 bytes); `/` and `/template-content.html` reference `images/logo.png` with **0** remaining `Resources/Wix` mentions; the old logo URL → **404**; `/farcry-6.html` → **200**.

---

## ✅ Fixed in iteration 5 (accessibility: image-compare slider is keyboard-operable)

### Accessibility (a11y)
- **The image-compare slider is now operable and announced without a mouse (P2 #7).** `initImageCompareSliders` was pointer-only (mousedown/touchmove). The slider is live content — it ships on `division-2.html` (5 instances) and `farcry-6.html` (1 instance) via the `image-compare` block. The handle is now exposed as a **`role="slider"`** with `tabindex="0"`, `aria-orientation="horizontal"`, `aria-valuemin="0"` / `aria-valuemax="100"`, a dynamic `aria-valuenow`, a descriptive `aria-valuetext` (`"N% before, M% after"`), and an `aria-label` built from the block's Before/After labels (e.g. *"Image comparison slider for Before and After"*). Keyboard support follows the WAI-ARIA slider pattern: **ArrowLeft/ArrowDown** and **ArrowRight/ArrowUp** nudge by 2%, **PageDown/PageUp** jump by 10%, **Home/End** snap to 0/100; all handled keys `preventDefault` so the page doesn't scroll while operating it, while Tab/Enter/Space pass through untouched.
- **Unified slider state across input methods.** Previously `setPosition()` moved the divider but didn't track position, so a mouse drag to 80% followed by an arrow key would jump back toward 50%. Introduced a single `currentPosition` variable that mouse, touch, and keyboard all read/write through `setPosition()`, so the three inputs can never desync. `aria-valuenow`/`aria-valuetext` are updated on every position change from any source.
- **Hid the decorative handle chevrons from AT (P2 #7, cont.).** The two-arrow SVG inside the handle is now `aria-hidden="true" focusable="false"` — its chevrons are decorative, and the handle's accessible name comes from the `aria-label`. (Without this, some screen-reader/browser combinations would have announced the inline SVG.)
- **Removed a redundant `aria-label` on a role-less element (P2 #8).** `<div class="tag-system-meta" aria-label="Tag system update reminder">` in `index.html` had no role, so the `aria-label` was ignored by AT — and the name ("Tag system update reminder") didn't even describe the visible content. The element's text nodes (the visible sentence + `<time>`) are already accessible, so the attribute was simply removed.
- **Visible focus ring on the slider handle.** Added `.image-compare-handle:focus-visible { outline: 2px solid var(--accent); outline-offset: 4px; }` so the keyboard-focus ring clears the handle's white box-shadow halo and stays legible. (The global `:focus-visible` ring already applies once the handle is focusable, but its 2px offset collided with the 3px halo.)

### Validation
- `node --check home.js`, `node --check project-instance-loader.js` — pass.
- Served locally (`python3 -m http.server 8771`): `/division-2.html` → **200** and references `project-instance-loader.js`; the served loader contains the new slider code (`role="slider"`, `aria-orientation`, `aria-valuetext`, `handle.addEventListener("keydown")` — 4/4 markers); served `style.css` contains `.image-compare-handle:focus-visible`; served `/` has **0** occurrences of the removed `aria-label`. Server confirmed stopped (`ss -ltn`).
- Note: the slider DOM is built at runtime by JS, so the `role="slider"` attributes only appear after `initImageCompareSliders` runs in a real browser; the attribute-setting and keydown wiring were verified by reading the code and confirming the served JS carries them (no jsdom/headless browser is available — there is no build/test system per `AGENTS.md`).

---

## ✅ Fixed in iteration 6 (architecture: extract `shared.js` — P5 #19, part 1)

### Architecture / maintainability
- **Extracted the byte-identical helpers duplicated across `home.js` and `project-instance-loader.js` into a new `shared.js`** (P5 #19). `CDN_BASE`, `toCdnUrl()`, `loadVersionTag()`, and `TAXONOMY_MANIFEST_PATH` were defined verbatim in both files. They now live once in `shared.js`, which is loaded as a classic `<script>` **before** the page-specific entry script on every page (`index.html` → before `home.js`; the four project pages → before `project-instance-loader.js`). Classic scripts share the global lexical scope, so top-level `const`/`function` in `shared.js` are visible to the subsequently-loaded entry script without `import`/`export` or a module-system migration; the duplicate declarations were removed from both consumers, so there is no "Identifier has already been declared" collision. The CDN base URL and the version-tag/GitHub-commit contract are now single-sourced — editing them in one place updates both page types.
- **Fixed a latent version-tag bug exposed by the extraction (bonus correctness).** `project-instance-loader.js` called `loadVersionTag()` at the very top of the file, but `.top-bar-version` lives inside the shared template that is fetched and injected *asynchronously* by `loadProjectInstanceTemplate()` — so on project pages the element did not exist when the call ran, the helper no-op'd, and **the version pill was never populated on any project page** (the homepage was unaffected because its pill is in the static HTML). Moved the call to *after* `root.append(header, overlay, sidebar, main)`, where the injected top bar is guaranteed to be in the DOM, so project pages now show their commit version too.

### Scope note — what was *not* extracted this iteration
P5 #19 also flags the sidebar-toggle and gallery-init logic as duplicated. Those were deliberately **deferred** because the two copies have meaningful behavioral divergence that a naive merge would regress, and there is no browser/jsdom harness to smoke-test the result:
- **Sidebar toggle.** `home.js`'s inline version additionally implements smooth-scrolling + `history.replaceState` for in-page hash links (`#works`, `#about`, …) and closes the drawer on nav; `project-instance-loader.js`'s `initSidebarToggle()` only handles open/close + mobile auto-close (project-page nav has no in-page anchors to scroll to). Merging requires parameterizing the hash-scroll behavior.
- **Gallery init.** `home.js`'s `initializeProjectGallery()` operates on a module-scoped `projectGallery` hydrated from `Resources/Project Template/pages`, while the loader's `initProjectGallery(main)` operates on gallery nodes injected from the template. The render/swipe bodies are near-identical but the entry points differ.
- **Image-compare init** lives **only** in the loader (not in `home.js`), so it was never actually duplicated — the original backlog line overstated it.

These remain the suggested next extraction iteration (see plan below).

### Validation
- `node --check shared.js`, `node --check home.js`, `node --check project-instance-loader.js` — pass; all project configs + `data/taxonomy.json` — valid JSON.
- Served locally (`python3 -m http.server 8787`): `/shared.js`, `/home.js`, `/project-instance-loader.js`, `/`, and all four project pages → **200**; `index.html` and every project page load `shared.js` on the line **before** their entry script; served `shared.js` contains all **4** helper definitions; served `home.js` and `project-instance-loader.js` contain **0** (no redeclaration); served loader still carries the post-injection `loadVersionTag();` call. Server confirmed stopped (`ss -ltn`).
- Caveat (same as prior iterations): runtime behavior of the shared-global handoff and the project-page version pill can only be confirmed in a real browser — there is no build/test system per `AGENTS.md` and no jsdom/headless browser available. Verified by `node --check`, serve+grep, and code reading; a manual click-through (`python3 -m http.server` → open `/` and a project page, confirm no console errors and the version pill renders on both) is the remaining confidence step.

---

## ✅ Fixed in iteration 7 (performance: non-render-blocking Inter font + a11y contrast audit)

### Performance
- **Made the Inter web font non-render-blocking on every page (P3 #11).** All five pages (`index.html` + the four project pages) and the shared `template-content.html` loaded the Google Fonts stylesheet synchronously in `<head>`, so first paint waited on a cross-origin CSS file that only carries font `@font-face` declarations (no layout). Converted the link to the standard async pattern: `media="print"` pulls it off the render-critical path, `onload="this.media='all'"` applies it the moment it arrives, and the existing `&display=swap` means text renders in the `system-ui` fallback immediately and re-renders in Inter on swap (a brief FOUT — acceptable and the recommended trade-off for LCP). A `<noscript>` mirror of the link keeps the font for no-JS visitors, since the `onload` swap cannot fire without JS (matters most on the homepage, which is fully usable without JS; project pages already require JS to render content). `style.css` is deliberately left render-blocking — it is the critical stylesheet. This removes one item from Lighthouse's "Eliminate render-blocking resources" audit on all pages.
- **Verified there is no CSP** that would block the inline `onload` handler (no `Content-Security-Policy` meta tag; GitHub Pages sets none).

### Accessibility (audit, no code change needed)
- **Ran a full WCAG 2.1 contrast audit (P3 #9 — was P2 #9) and all combinations pass.** Computed exact contrast ratios (proper sRGB→linear-luminance math, including rgba backgrounds composited over their real panel) for every text token (`--text #eff2ff`, `--muted #b5bedf`, `--accent #78a8ff`, `--accent-2 #a28bff`) against every background it actually sits on (`--bg`, `--panel`, `--panel-2`, `rgba(0,0,0,.16)` sub-cards, `rgba(255,255,255,.06)` chips, the `rgba(15,17,26,.88)` top bar), plus the dark-text-on-light cases (skip-link / CTA button = `--bg` on `--accent` at 7.93:1; slider handle = `--bg` on `--text` at 16.87:1). **Every pairing clears WCAG AA** (≥4.5:1 for normal text); the tightest real-world pairing is `--accent-2` on the chip background at 5.37:1, still above threshold. Conclusion: the palette is AA-compliant; no token changes required. (Audit script: sRGB channel linearization with the 0.03928 threshold, luminance via the standard Rec.709 weights, ratio = (L_lighter+0.05)/(L_darker+0.05).)

### Validation
- `node --check shared.js/home.js/project-instance-loader.js` — pass; `data/taxonomy.json` + all 4 project configs — valid JSON (no code in those files changed; sanity check).
- Served locally (`python3 -m http.server 8801`): `/`, all four project pages, `/template-content.html`, `/style.css` → **200**; every page's served HTML carries exactly one async font link (`media="print"` + `onload="this.media='all'"`) and one `<noscript>` fallback; the font URL appears exactly twice per page (async link + noscript mirror) with no remaining render-blocking font `<link>`. No CSP meta tag present. Server confirmed stopped (`ss -ltn`).
- Caveat: the FUT/swap timing can only be observed in a real browser; verified by serve+grep and reading the served `<head>`. A manual load confirming text appears in the fallback then swaps to Inter is the remaining confidence step.

---

## ✅ Fixed in iteration 8 (correctness bug: broken featured-recommendation image)

### Correctness
- **Fixed a malformed R2 object URL that 404'd a live featured image (new finding).** In `Resources/Project Instances/config/farcry6-procedural-generation.json`, the `full_02` entry was missing the dot before its extension — `…/featured/HighResGameImage02jpg` — while all five siblings (`full_01`, `full_03`–`full_06`) and all six `thumb_0N` entries were well-formed `*.jpg`. The loader uses `full_NN` as the high-resolution preview shown when a visitor clicks a thumbnail (`button.dataset.gallerySrc = config.images[fullRole] || imagePath`, `project-instance-loader.js:640–646`), so on the **Far Cry 6** page clicking thumbnail #2 requested a non-existent R2 object and rendered a broken image in the main preview pane. Corrected the extension: `HighResGameImage02.jpg`. This was an isolated typo, not a systemic naming problem — a recursive scan of every string value across all four project configs + `data/taxonomy.json` for missing-dot image extensions returns **0** other suspects (it only ever matched this one).
- **Decision not to "fix" `CenimaticShader1.png` (content note, not a code change).** While auditing the Far Cry 6 config I noticed the block image path `projects/farcry6-procedural-generation/blocks/CenimaticShader1.png` is a misspelling of "Cinematic." Deliberately **left it untouched**: it is the literal filename of an object uploaded to the R2 bucket, and the visible `alt` text (`"Far Cry 6 cinematic shader pass"`) is correctly spelled, so only an invisible URL carries the typo. Renaming the URL would point at an object that doesn't exist and reintroduce the exact 404 class of bug just fixed. The right remediation is to re-upload the asset with the corrected filename in R2 — not verifiable from the repo, so deferred.

### Validation
- All four project configs + `data/taxonomy.json` parse as valid JSON (`python3 -c json.load`).
- Recursive missing-dot-extension scan over every config string value: **0** suspects.
- Served locally (`python3 -m http.server 8841`): `/Resources/Project Instances/config/farcry6-procedural-generation.json` → **200**; the served `full_02` line reads `HighResGameImage02.jpg`; **0** occurrences of `HighResGameImage02jpg` in the served bytes. Server confirmed stopped (`ss -ltn`).
- Caveat: the rendered high-res preview swap is a runtime DOM/JS behavior; verified by `node`-free JSON validity + serve+grep + code reading of the loader's `dataset.gallerySrc` assignment. A manual click-through (open `/farcry-6.html`, click thumbnail 2) is the remaining confidence step, but the data-level fix is unambiguous.

---

## ✅ Fixed in iteration 9 (SEO: JSON-LD structured data on JS-rendered project pages)

### SEO
- **Added schema.org `Article` JSON-LD structured data to all four project pages (part of P1 #1).** `project-instance-loader.js` builds each project page's entire `<body>` at runtime (fetch `template-content.html` + per-project JSON, construct DOM in JS), so search engines that fetch-and-parse without executing JavaScript see an empty document — only the `<head>` meta tags existed. Google *does* eventually render JS, but that is a deferred, secondary pass; structured data in the static HTML is parsed on the first, immediate crawl. Each page (`farcry-6.html`, `division-2.html`, `d-walker-vs-sahelanthropus.html`, `raiden-vs-gekko.html`) now carries a `<script type="application/ld+json">` block in its `<head>` with the project's real facts: `headline` (canonical project title), `description` (the same polished copy as the page's `<meta name="description">`), `image` (the cover, identical to `og:image`), `url` (identical to `<link rel="canonical">`), `mainEntityOfPage` (`@id` = canonical), `author` and `publisher` (`Person` → Yujia Max Liu, self-published portfolio), and `keywords` (the real tool/language/role terms from the config — e.g. *Houdini, Substance Designer, VEX, HLSL* for Far Cry 6). This gives crawlers rich, machine-readable content for the JS-rendered pages without touching the working renderer or adding a build step — the pragmatic SEO win short of full static pre-rendering.
- **Schema-type choice — `Article`, no fabricated dates.** These are authored technical-art case studies, so `Article` (well-supported by Google's rich results) is the most semantically useful type; `SoftwareApplication` was rejected because these are not downloadable apps. `datePublished` was **deliberately omitted** rather than fabricated: the configs carry only a work-period string (`time`, e.g. *"Sep 2020 – Dec 2021"*, *"2022-2024"*), not a page-publication date, and a Technical Director review should not invent dates that Google would surface as freshness signals — that is a real data gap worth filling later (see updated P1 #1). The block is otherwise fully populated, so the omission costs only Article-rich-result eligibility, not indexing or entity understanding.

### Validation
- Extracted each `<script type="application/ld+json">` block and `json.load`'d it — **all four parse as valid JSON**; each page has **exactly one** block; every required key (`@context`, `@type`, `headline`, `description`, `image`, `url`, `author`, `publisher`, `mainEntityOfPage`, `keywords`) is present; `@context` = `https://schema.org`, `@type` = `Article`, `author`/`publisher` are `Person`.
- **Internal consistency cross-check (served):** for each page the JSON-LD `url` equals the `<link rel="canonical">` href, and the JSON-LD `image` equals the `og:image` — so structured data, canonical, and social preview all agree (4/4).
- Served locally (`python3 -m http.server 8855`): all four pages → **200** and serve their JSON-LD block. Server confirmed stopped (`ss -ltn`). No JS/CSS changed (`git status` shows only the four HTML files; `node --check` on all three JS files still passes).
- Caveat: Google's Rich Results Test (run against the deployed `maxlyj.com` URL post-deploy) is the remaining confidence step to confirm the structured data is accepted and surfaces the expected warnings (the intentional `datePublished` gap). The data is valid JSON-LD and internally consistent, so this is a deploy-time confirmation, not a correctness risk.

---

## ✅ Fixed in iteration 10 (architecture: dead-code sweep — P4 #18, P1 #4)

### Architecture / maintainability
- **Deleted the dead legacy `projectDetails` fallback in `renderProjectDetails` (P4 #18).** *(The iteration-9 notes mis-numbered this "P5 #18"; it is item #18 under the P4 — Content/IA heading above.)* The function rendered project detail blocks from `config.projectDetails.blocks` and then carried a ~60-line backward-compat fallback that re-rendered the legacy `initiative` / `pipeline` / `result` / `placeholderImage` / `placeholderVideo` shape if `blocks` was absent. Iteration 9 confirmed **0 of 4 shipped configs carry any legacy key** (all use `blocks`), so the entire fallback branch was unreachable. Removed it (and the now-pointless `return;`), leaving a one-line comment recording that `blocks` is the only supported shape. Net: the function does exactly one thing, with no second rendering path to keep in sync with `createDetailBlockElement`.
- **Deleted the dead `[data-project-template-folder]` template-hydration block from `home.js` (P1 #4 — resolved by deletion).** This was the largest piece of dead code in the repo: ~144 lines (`canLoadProjectImage` / `resolveProjectImagePath` runtime image-extension probing, `hydrateProjectTemplateImages`, and an `initializeProjectGallery` copy that duplicated the loader's `initProjectGallery`) gated behind `if (projectTemplateMain)`. The trigger element `[data-project-template-folder][data-project-template-slug]` exists **only** in `template-content.html`, which is not in `sitemap.xml`, is linked from nowhere, and is consumed by `project-instance-loader.js` via `fetch()` + `DOMParser` (whose `<script>` tags do not execute). `home.js` only ships on `index.html`, where the element does not exist — so the block **never ran on any shipped page**. Verified the helpers it defined (`buildProjectImagePath`, `canLoadProjectImage`, `resolveProjectImagePath`, `hydrateProjectTemplateImages`, `initializeProjectGallery`) were referenced nowhere outside the block, then removed the block plus its two now-orphaned module-scope symbols (`RECOMMENDATION_IMAGE_EXTENSION_PRIORITY`, the `projectGallery` query), leaving a brief comment at the deletion site explaining *why* home.js no longer hydrates the template. This retires the P1 #4 runtime image-extension probing concern outright (option (a) of the re-scope) rather than merely documenting it.
- **Removed an orphaned, mid-sentence comment block at the tail of `project-instance-loader.js` (new finding).** A leftover `/* … after root.append(header, overlay, sidebar, main). At that point the elements are guaranteed … */` fragment (orphaned during the iteration-6 `shared.js` extraction) sat between `initSidebarToggle()` and the top-level `loadProjectInstanceTemplate()` call — it started mid-sentence and duplicated the in-function comment at the real call site (loader ~lines 691–694). Deleted it.

### Net effect
- **~205 lines of confirmed-dead code removed** across the two entry-point scripts (`home.js` 710 → 570; `project-instance-loader.js` 883 → 818) with **zero shipped behavior changed**: every shipped `home.js` feature (tag system, sidebar, featured carousel) lives above the deleted block and was untouched, and the loader's working render / gallery / error paths are intact.

### Validation
- `node --check shared.js`, `node --check home.js`, `node --check project-instance-loader.js` — **pass**.
- Symbol accounting: **0** remaining references in `home.js` to `projectTemplateMain` / `hydrateProjectTemplateImages` / `RECOMMENDATION_IMAGE_EXTENSION_PRIORITY` / `canLoadProjectImage` / `resolveProjectImagePath`; **0** references in the loader to `config.projectDetails?.initiative|pipeline|result` / `placeholderImage|placeholderVideo`; **0** occurrences of the orphaned comment string. Cross-file check: none of the deleted symbols are referenced in `shared.js` or the sibling entry point.
- Served locally (`python3 -m http.server 8899`): `/`, `/index.html`, `/home.js`, `/shared.js`, `/project-instance-loader.js`, and both project pages → **200**; served `home.js` still carries every shipped feature marker (`renderTagSystem`, `buildFeaturedPages`, `movePage`, `closeSidebar`, `loadTaxonomyManifest`, `FEATURED_PROJECT_SLUGS`); served loader still carries `renderProjectDetails` / `initProjectGallery` / `renderProjectLoadError` / `loadProjectInstanceTemplate` and the cover hydration; `renderProjectDetails` now references `projectDetails?.blocks` with no `Backward compatibility` branch. All four project configs + `data/taxonomy.json` — valid JSON. Server confirmed stopped (`ss -ltn`).
- Caveat (same as prior iterations): there is no build/test system (per `AGENTS.md`) and no jsdom/headless browser, so the absence of runtime regressions is verified by `node --check` + symbol accounting + serve+grep + code reading. Because the removed code was provably unreachable on shipped pages (no shipped page both loads `home.js` and contains the trigger element), a manual click-through cannot exercise the deletion — it can only confirm the shipped paths still work, which the marker grep above establishes.

---

## ✅ Fixed in iteration 11 (content correctness: placeholder media shipping on d-walker / raiden pages)

The planned iteration-11 item was P1 #1 option (a) (static pre-render). That was deferred when this review pass surfaced a higher-priority **content-correctness** defect — two of the four live project pages were shipping template-scaffold placeholder media — which "critical bugs first" says must precede SEO polish. (You cannot meaningfully SEO-optimize a page that renders a `placehold.co` image and a broken YouTube embed.)

### Correctness — removed shipping placeholder content
- **Removed raiden's placeholder Project-Details blocks.** `Resources/Project Instances/config/raiden-vs-gekko.json` carried two blocks that were never replaced with real media and rendered on the live `/raiden-vs-gekko.html` page:
  - an **image** block pointing at `https://placehold.co/1280x720?text=Raiden+Project+Details+Image` (an external placeholder-service graphic; the `alt` literally read *"Raiden project details placeholder image"*), and
  - a **video** block pointing at `https://www.youtube.com/watch?v=VIDEO_ID_PLACEHOLDER` (an invalid YouTube ID → a broken embed; the `title` literally read *"Raiden project details placeholder video"*).

  Both deleted. The raiden page now shows only its real authored content: the three text sections (Initiative / Research Pipeline / Research Result) plus its real cover and gallery. Real media can be re-added when available — the gap is recorded as P1 #24. Same defect class as the iteration-8 `HighResGameImage02jpg` typo: a config value that ships broken/placeholder media because the loader trusts it verbatim (`createDetailBlockElement` renders any `image`/`video` block whose `src`/`url` is present — a malformed or placeholder value is not caught).

### Verified real (no change made) — covers + d-walker video
Before editing I confirmed which d-walker/raiden assets are genuine so as not to "fix" non-defects:
- **d-walker cover** = R2 `projects/d-walker-vs-sahelanthropus/cover.webp` — a real 1440×810 Unreal Engine render (a mech scene fitting "D-Walker VS Sahelanthropus"), confirmed visually. (An earlier sweep turned up `fr_d-walker-vs-sahelanthropus__main.svg` — a placeholder — but that file is **unreferenced scaffold**, not the cover; the cover key is already the real R2 URL.)
- **raiden cover** = local `fr_raiden-vs-gekko__main.png`, **byte-identical (140646 B, 429×241)** to the canonical R2 `projects/raiden-vs-gekko/cover.png` the homepage already uses — i.e. the intended cover image (verified real rendered artwork), just served from a repo path instead of R2. Left as-is.
- **d-walker video** = real YouTube ID `Go_2zuEATvg`. Left as-is.

### Documented (not fixed — needs real assets): placeholder gallery thumbnails
- **d-walker & raiden `thumb_01..04` are template-scaffold placeholder SVGs** (filed as **P1 #24**). Both configs point the four gallery thumbnails at `Resources/Featured Recommendations/pages/fr-*/fr_*__thumb_0N.svg` — 476-byte scaffold files that render the literal text *"Thumbnail 0N"* / *"fr_template-page__thumb_0N.svg"* on a colored rectangle; the sibling `fr_*__main.svg` files are the same scaffold (labeled *"Template Main Placeholder"*). These are the unreplaced output of the featured-recommendation scaffold (`Skills/featured-recommendation-page/SKILL.md`, step 3: *"Replace placeholder images while preserving main and thumb_01..04 roles"*). The AAA game pages (Far Cry 6, Division 2) use real R2 `featured/LowResGameImage0N.jpg` thumbnails, but these two short-film projects were never populated.
- **No real thumbnail variants exist on R2** to repoint to — probed `projects/<slug>/featured/LowResGameImage01.jpg`, `projects/<slug>/thumb_0N.{jpg,png}`, `projects/<slug>/featured/thumb_01.jpg` for both slugs → all **404**. Only the covers exist on R2 (d-walker `cover.webp` and raiden `cover.png` both 200).
- **Decision not to duplicate the cover into the thumbnails.** Considered repointing `thumb_01..04` at each project's real cover to eliminate the placeholder graphics, but a gallery showing the same hero image four times reads as a broken gallery (to visitors and to automated tooling) — a worse outcome than scaffolding that clearly signals "image needed." The correct fix is real thumbnail assets; left the placeholders and documented.

### Validation
- All 4 project configs + `data/taxonomy.json` parse as valid JSON (`python3 -c json.load`).
- raiden `projectDetails.blocks`: **8 → 6** (3× `h3` + 3× `p`); **0** placeholder markers remain.
- Repo-wide grep of shipped configs for `placehold.co` / `VIDEO_ID_PLACEHOLDER` → **0** hits.
- R2 HTTP checks: d-walker `cover.webp` **200 image/webp**, raiden `cover.png` **200 image/png** (covers exist); thumbnail probes → **all 404** (no real thumbs to repoint to). Covers visually confirmed as real rendered artwork via image analysis.
- JS unchanged this iteration (`node --check shared.js / home.js / project-instance-loader.js` still passes — sanity only).
- Caveat: the rendered raiden page (block removal) is a runtime DOM behavior; verified at the data level (JSON validity + marker grep + code reading of `createDetailBlockElement`). A manual load of `/raiden-vs-gekko.html` confirming the placeholder image/embed are gone and the three text sections remain is the remaining confidence step; the data-level removal is unambiguous.

---

## ✅ Fixed in iteration 12 (SEO: static pre-render fallback for JS-rendered project pages — P1 #1, option a)

The headline SEO gap from iteration 1: every project page builds its entire `<body>` at runtime in `project-instance-loader.js`, so a no-JS fetch (and a crawler's immediate first pass, before Google's deferred JS-rendering pass) saw an empty document — only the `<head>` meta + the iteration-9 JSON-LD carried the project's facts. Closed with a build-time pre-render.

### Architecture
- **Added `scripts/prerender-project-pages.js`** — a dependency-free Node script (run pre-deploy, `node scripts/prerender-project-pages.js` from the repo root) that reads each project config (`Resources/Project Instances/config/<slug>.json`) + `data/taxonomy.json` and inlines the project's **real content** as semantic HTML inside a `<noscript>` block in each of the four project pages. The generated block is delimited by `BEGIN/END project-noscript` marker comments; the script strips any prior generation **and** the legacy bare `This page requires JavaScript` `<noscript>` before re-inserting ahead of `<script src="shared.js">`, so it is **idempotent** (re-running produces byte-identical output — verified). It reads `CDN_BASE` from `shared.js` (regex) so the build tool and the runtime share one source of truth, and fails loudly if the constant can't be found.
- **What gets inlined per page:** a back-to-home link (no site nav exists without JS — the loader injects the sidebar/top bar too), kicker, `<h1>` title, description, the cover image, a `<dl>` metadata grid (Tools / Languages / Period / Role / Tags resolved from the taxonomy), **every** `projectDetails.blocks` entry rendered to its semantic equivalent (`h2/h3/p` as-is; `image` → `<figure><img>` with alt; `image-compare` → a `<figure>` of both before/after images + caption; `video` → a text link to the source URL), and a closing note explaining the interactive version needs JS. This is substantial, real, crawlable body text (e.g. division-2 inlines 40 blocks / 23 figures).
- **Added a `.project-noscript` style block to `style.css`** (≈155 lines, design-token-based: `--text/--muted/--accent/--accent-2/--panel/--panel-2/--line`, responsive at ≤640px) so the text fallback reads like the rest of the site for no-JS visitors. It only ships in `style.css` (always loaded); it is inert for JS visitors because `<noscript>` content is never rendered when scripting is on.

### Design decision — `<noscript>`, not primary visible `<body>`
The review's option (a) literal text says "inline into the page `<body>`." A primary-body pre-render (content inside `[data-project-instance-root]`, which the loader clears on success) would carry marginally more SEO weight, but it has two failure modes I **cannot verify without a browser** (there is no build/test system per `AGENTS.md`, no jsdom/headless browser): (1) a flash of unstyled pre-rendered content for JS visitors during the async fetch window (content appears, is wiped, then re-rendered — worse than the current blank→render), and (2) any edge case where the content isn't cleared risks duplicating it in the rendered DOM. The `<noscript>` approach has **none** of these risks: the HTML5 parser treats `<noscript>` content as RAWTEXT when scripting is enabled (it is never added to the DOM as elements, so it cannot duplicate, flash, or interfere with the loader's `querySelector`/`innerHTML`), and renders it as normal flow content for no-JS visitors and non-JS crawlers. Its only failure mode is "does nothing extra" — the principled call when you can't smoke-test. Google indexes `<noscript>` content; combined with the existing per-page meta + JSON-LD, the pages now have comprehensive static SEO coverage. A primary-body SSR/hydration refactor remains the longer-term enhancement (noted in P1 #1).

### Correctness — bug found and fixed during the build
- **The cover is assigned verbatim by the loader.** First run of the script applied `toCdnUrl()` to the cover and produced a broken R2 URL for raiden (`https://…r2.dev/Resources/Featured Recommendations/pages/…/fr_raiden-vs-gekko__main.png`) — raiden's cover is a **local repo path**, not an R2 path. The loader assigns the cover raw (`coverImage.src = config.images.cover;`, line 550 — no `toCdnUrl`), unlike block images which *do* use `toCdnUrl`. Corrected the script to mirror the loader exactly: cover = raw verbatim; only `image`/`image-compare` block sources get `toCdnUrl`. (Three covers are full R2 URLs and pass through either way; only raiden's local path was affected.)

### Validation
- **Idempotency:** md5'd all four pages, re-ran the script, md5'd again → **byte-identical** (4/4). Re-running is safe and the committed output is stable.
- `node --check scripts/prerender-project-pages.js`, `shared.js`, `home.js`, `project-instance-loader.js` — **pass**; all four project configs + `data/taxonomy.json` — valid JSON (configs/JS untouched by the script).
- Served locally (`python3 -m http.server 8910`): all four pages, `style.css`, `scripts/prerender-project-pages.js` → **200**; each served page carries the generated `<noscript>` with real project text (e.g. *"Procedural World Generation"* on farcry, the d-walker video ID `Go_2zuEATvg`, image-compare figures on division-2); the legacy *"requires JavaScript"* noscript is gone (**0/4**); `style.css` ships **25** `.project-noscript` rule references. Structure verified: the body noscript is a **sibling** of `[data-project-instance-root]` and does **not** wrap the scripts, so the loader's `root.innerHTML = ""` only ever clears the empty mount. Server confirmed stopped (`ss -ltn`).
- RAWTEXT-safety grep: **0** `noscript` substrings in any config/taxonomy value (so no config text can prematurely close the `<noscript>` when parsed as RAWTEXT with JS on). HTML-escaping verified (d-walker's `"` → `&quot;`); any `<` in config text would escape to `&lt;`.
- `index.html` **unchanged** (the homepage is already static HTML — no pre-render needed).
- Caveat: rendered no-JS appearance (styling/layout, image loading) can only be confirmed in a real browser with JS disabled; verified by serve+grep + code reading + idempotency. A manual check (`curl` the page, or a browser with JS off) confirming the real text/images render is the remaining confidence step; the data-level content is unambiguous and the runtime JS path is provably untouched.

---

## ✅ Fixed in iteration 13 (architecture + docs: template fragment source de-mystified, AGENTS.md staleness corrected — P5 #20)

Two related correctness/maintainability defects, both about accurately documenting the dual-JS / template-fragment architecture, fixed together.

### Architecture — `template-content.html` is now an honest fragment source (P5 #20)
- **Removed the dead `<script src="home.js"></script>`** at the end of `template-content.html`'s `<body>`. It was unambiguously dead in production: the file's sole consumer is `project-instance-loader.js`, which `fetch()`es it and parses it with `DOMParser.parseFromString(html, "text/html")` — and DOMParser-created documents have scripting disabled, so `<script>` elements in the parsed document are **never executed** (loader lines 499–504). It was also misleading: it implied `home.js` drives the template, but (a) `home.js` only ships on `index.html`, and (b) the template-gallery hydration block `home.js` *used* to run on this file was deleted in iteration 10. In production the sidebar/menu toggle is wired by the loader's own `initSidebarToggle()` after injection (loader line 636), not by `home.js`. Removing the tag changes **zero** production behavior (it never ran) and eliminates the false implication.
- **Added a banner comment** at the top of `template-content.html` documenting exactly what the file is: a non-shipped fragment source (not in `sitemap.xml`, linked from nowhere) whose only consumer extracts four nodes — `<header class="top-bar">`, `.sidebar-overlay`, `nav.sidebar`, `main.project-template-main` — via fetch+DOMParser; that the `<head>` is for **local browser preview only** and is never applied to a shipped page (each project page carries its own `<head>`); and that the sidebar toggle is wired by the loader, hence no script tag. This closes the "edit the template `<head>` thinking it propagates to project pages" confusion trap that P5 #20 flagged.
- **Decision — document the `<head>` rather than strip it.** P5 #20 offered "strip it or use it." Stripping the `<head>` would turn the file into a bare fragment and rely on DOMParser's implicit `<html><head><body>` error-recovery wrapping — a behavioral change to a file consumed at runtime on every project-page load, which I cannot browser-smoke-test (no build/test system per `AGENTS.md`, no jsdom/headless browser). Documenting it (banner comment + dead-script removal) achieves the clarity goal with **zero** parse-behavior change: the loader's four `querySelector`s still match the same four elements (verified), and the file remains a valid, locally-previewable HTML document. Same "principled call when you can't smoke-test" reasoning as the iteration-12 `<noscript>` decision.
- **Corrects a self-introduced no-op.** Iteration 7 converted the Inter font link to async on `template-content.html`'s `<head>` believing it helped project pages. It did nothing — the `<head>` is never applied (project pages get Inter from their own `<head>`, fixed in iteration 1). The async link is retained because it makes *local preview* accurate (Inter renders), but the banner now states plainly that it is preview-only.

### Docs — `AGENTS.md` was stale (described code deleted in iterations 6 & 10)
`AGENTS.md` is the onboarding doc, and it had accumulated three stale entries that actively mislead any contributor (or future review iteration) about how the site works:
- **"Dual JS Architecture"** claimed the homepage is "`index.html` + `template-content.html`" (the template is not a homepage) and that "`home.js` has template-gallery hydration code for `template-content.html`… `data-project-template-autohydrate="false"`" — but that hydration code was **deleted in iteration 10**. Rewritten to describe the real architecture: `index.html`→`home.js`; project pages→`project-instance-loader.js`; `shared.js` before both; `template-content.html` as a fragment source consumed via fetch+DOMParser. (Also recorded that `data-project-template-autohydrate` is now a **vestigial attribute**: the loader still sets it — line 620 — but **nothing reads it**, confirmed by a repo-wide grep. Left in place rather than removed because the loader is a runtime file I can't smoke-test and the attribute is harmless; documented as safe-to-ignore.)
- **"Featured Recommendations"** described a `fr-<slug>` folder + runtime `new Image()` extension-probing system (`png→jpg→svg`) and a "hardcoded `featuredPages` array (~line 209)." None of that matches shipped code: the carousel is driven by the `FEATURED_PROJECT_SLUGS` constant + `loadFeaturedProjectConfig(slug)` (which fetches the **same** `Resources/Project Instances/config/<slug>.json` the project page uses), and the extension probing was **deleted in iteration 10**. Rewritten to match the shipped mechanism. (The `Resources/Featured Recommendations/pages/fr-<slug>/` folders still exist as image-asset sources — d-walker/raiden configs point `cover`/`thumb_01..04` at files inside them — but they are not a separate data path; clarified.)
- **Project-config schema** claimed "`Legacy: projectDetails.initiative/pipeline/result/placeholderImage/placeholderVideo` still supported as fallback" — that fallback was **deleted in iteration 10** (all 4 shipped configs use `blocks`). Replaced with a note that `blocks` is the only supported shape.

### Validation
- `node --check shared.js / home.js / project-instance-loader.js / scripts/prerender-project-pages.js` — **pass** (JS untouched; sanity).
- **Runtime-equivalence (the loader's extraction is unaffected):** the loader still `fetch("template-content.html")` (line 499) and its four `querySelector`s (lines 514–517) are unchanged; the banner comment closes before `<html>` (verified) so it is pure comment content; a tolerant parse confirms all four target elements are still present as real elements. The file's tag balance is intact (`<html>`/`</html>`, `<body>`/`</body>` each 1:1).
- Served locally (`python3 -m http.server 8923`): `/template-content.html`, `/farcry-6.html`, `/AGENTS.md`, `/home.js`, `/shared.js`, `/project-instance-loader.js` → all **200**; served `template-content.html` has the banner (1), **0** `<script>` tags, and the real `<header>`/`<main>` elements present. Server confirmed stopped (`ss -ltn` → 0 listeners).
- AGENTS.md stale-claim greps → **0** for: "template-gallery hydration code for", "probed at runtime via `new Image`", "still supported as fallback", "featuredPages array (~line 209)".
- Caveat (same as prior iterations): rendered DOM behavior of a runtime-consumed file can only be confirmed in a real browser; verified by `node --check` + serve+grep + a tolerant-parse simulation of the loader's fetch+DOMParser+querySelector path. The change is provably extraction-neutral (the four selectors match the same four unchanged elements), so the runtime render is untouched.

---

## ✅ Fixed in iteration 14 (performance: prioritize the LCP hero image + reserve the logo box — P3 #12, re-scoped & resolved)

The iteration-13 plan offered three browser-free candidates. Per the objective's priority order (performance before content/architecture), I took **P3 #12 (CLS)** — but first re-audited it honestly, because a naive read of the backlog line ("No `width`/`height` on JS-generated card images → potential CLS") would have over-stated the gap.

### Audit — most images already reserve space; two genuine gaps remained
I enumerated every image on the homepage and checked each against its CSS:
- **Work-card images** (`.card img`, JS-generated by `createWorkCard` / `renderRelatedWorks`) — already reserved by a **fixed `height: 230px`** (`210px` on mobile) + `object-fit: cover`. The box is 230px tall the moment the card enters the DOM, before the image bytes arrive. **No per-image CLS.** So P3 #12's literal "card image CLS" concern is already mitigated by the existing fixed-height CSS — not a real gap.
- **Featured carousel main image + thumbnails** (`.fr-media-main img`, `.fr-thumb`) — already reserved by `aspect-ratio: 16 / 9`. **No CLS.**
- **Project-page cover / detail / image-compare images** (`.project-cover`, `.project-details-image`, `.image-compare-after/before`) — all already carry `aspect-ratio: 16 / 9`. **No CLS.**
- **About portrait** (`index.html`) — got `width`/`height` + `fetchpriority="high"` in iteration 1. **No CLS, already LCP-prioritized.**
- **Brand logo `<img>`** (`index.html`) — ❌ **no `width`/`height`**, and `.brand img` sets `height: 34px; width: auto` with **no `aspect-ratio`**. The browser couldn't reserve the correct-width box until the image bytes arrived → a small CLS on the always-visible top-bar logo.
- **Featured hero `#fr-main-image`** (`index.html`) — ❌ **no `fetchpriority="high"` and no explicit `width`/`height`**. This is the homepage's largest above-the-fold image and the strongest LCP candidate, yet the iteration-1 LCP-priority treatment was applied to the *portrait*, not to the *hero*.

So the genuine, residual gaps were exactly two: the **logo box reservation** and the **hero image LCP priority**. Both fixed.

### Performance
- **Prioritized the LCP hero image.** Added `fetchpriority="high"` to `#fr-main-image`. The browser now fetches the hero at high priority instead of default (low) priority competing with the Inter font, the taxonomy fetch, and the neighbor-image preload — a measurable Largest-Contentful-Paint improvement (the Lighthouse "prioritize LCP element" opportunity). Safe because the static `src` (Far Cry 6 cover) is identical to the first carousel entry's cover (aligned in iterations 1–2), so the high-priority fetch is never wasted on an image that gets immediately swapped. (When the visitor later navigates the carousel, `home.js` reassigns `frMainImage.src`; keeping `fetchpriority="high"` on the element means whatever cover is *currently shown* stays fast to load — desirable, since only one hero is visible at a time.)
- **Added explicit `width="1280" height="720"` to the hero.** These match the CSS-enforced `aspect-ratio: 16 / 9` (the CSS governs final layout: `width: 100%`, `height: auto`, `object-fit: cover`); the attributes are the Lighthouse-aligned "explicit width/height" reservation hint + a pre-CSS-layout fallback. (R2 is unreachable from the sandbox, so the source cover's exact pixels weren't fetchable — but `1280×720` is the same 16:9 ratio the CSS renders regardless of the source dimensions, so it is correct as an intrinsic hint.)

### Correctness — small CLS on the always-visible logo
- **Reserved the brand-logo box.** Added `width="119" height="60"` (the file's real intrinsic dimensions, from `file images/logo.png`) to the `.brand` logo `<img>`. `.brand img` renders at `height: 34px; width: auto` (≈67.5px wide) with no `aspect-ratio`, so the width/height attributes are now the ratio source the browser uses to reserve the full box on first layout — eliminating the small horizontal snap the logo did while its bytes loaded. CSS still governs final rendering (34px tall), so there is **zero visual change** once painted.

### Validation
- `node --check home.js / project-instance-loader.js / shared.js` — **pass** (JS untouched this iteration; sanity only).
- Served locally (`python3 -m http.server 8941/8942`): `/` → **200**; the served hero `<img id="fr-main-image">` carries `decoding="async" width="1280" height="720" fetchpriority="high"`; the served logo `<img>` carries `width="119" height="60"` (multiline assert). Server confirmed stopped (`ss -ltn` → no listener).
- No-CLS regression argument: every homepage image now either has explicit `width`/`height` (portrait, logo, hero) or is reserved by CSS `aspect-ratio` / fixed height (thumbs, work cards) — so Lighthouse's "image elements do not have explicit width and height" audit is clear for the above-the-fold images, and the LCP element is high-priority.
- Caveat: the actual LCP/LCP-priority delta can only be quantified in a real browser with network (Lighthouse / WebPageTest against the deployed `maxlyj.com`); verified here by serve+grep + code reading + the CSS-vs-attribute reasoning above. The hero's static `src` is stable across hydration (no wasted high-priority fetch), so the change is provably safe at runtime.

---

## ✅ Fixed in iteration 15 (architecture/SEO: JSON-LD generated from config — deduping the 4 hand-maintained blocks)

The iteration-14 plan listed *"extend `scripts/prerender-project-pages.js` to also generate the per-page JSON-LD from the config (deduping it vs. the four hand-maintained blocks added in iteration 9, which can drift)"* as a candidate. An honest pre-audit reframed the drift risk before implementing — and the reframing is the point.

### Audit — the JSON-LD values *deliberately* differ from the config's in-page fields
I checked how each hand-maintained JSON-LD field relates to the existing config (you can only "generate from the config" if the values can live there):
- **`headline`** ≠ `config.title` for Division 2 (JSON-LD *"The Division 2 — Technical Art Tools"* vs. config title *"Division 2 Tools Showcase"*); the other three match `config.title`.
- **`description`** ≠ `config.description` for all four — it's the polished SEO copy (e.g. ends *"…A Ubisoft Shanghai technical-art case study."*), which is also the `<meta name="description">` (verified equal 4/4), distinct from the in-page hero description.
- **`image`** ≠ `config.images.cover` for two pages: Far Cry 6 (JSON-LD `cover.jpg` vs. config cover `featured/Header_Farcry6.jpg`) and raiden (JSON-LD absolute R2 `cover.png` vs. config cover `Resources/Featured…/fr_raiden-vs-gekko__main.png` — a local path that would be a broken relative `og:image`).
- **`keywords`** is hand-curated (title split + tools/languages de-duped + kicker terms), present nowhere else.

So the JSON-LD legitimately needs its **own** SEO values, and they cannot be *derived* from existing config fields. The genuine defect isn't "JSON-LD drifted from the config" (the differences are intentional) — it's that **raw JSON was hand-typed into HTML across four files**: the most error-prone SEO artifact in the repo (one stray comma breaks it; `@context`/`@type`/`author`/`publisher` boilerplate duplicated 4× with no validation; editing structured data = hand-editing JSON-in-HTML). Single-sourcing into typed config fields + a `JSON.stringify`-validated generator eliminates that whole class of problem.

### Architecture
- **Added a `seo` block to each of the four project configs** (`headline`, `description`, `image`, `keywords`), populated **verbatim** from the current hand-maintained JSON-LD — so **zero SEO copy changes**; every value is preserved exactly, including the deliberate differences from `config.title` / `config.images.cover`. Verified the runtime consumers (`project-instance-loader.js`, `home.js`) access config by named fields only (no `Object.keys` / `for…in` enumeration), so an extra key is inert at runtime.
- **Extended `scripts/prerender-project-pages.js`** to also generate the per-page schema.org `Article` JSON-LD block in the `<head>` from `config.seo`: `headline`/`description`/`image`/`keywords` from the config; `url` **derived** from the site base (read from `CNAME`) + the page filename (so it always matches `<link rel="canonical">` without per-page maintenance); fixed `author`/`publisher` (`Person` → Yujia Max Liu). Emitted via `JSON.stringify(obj, null, 2)` — the output JSON is **guaranteed valid**, replacing four hand-maintained JSON-in-HTML blocks. Idempotent `BEGIN/END project-jsonld` markers (same proven model as the `<noscript>` block): the script strips any prior generated block **and** the iteration-9 hand-maintained block (its explanation comment + raw `<script>`) before re-inserting a fresh block ahead of the Google Fonts `preconnect`, so re-runs are byte-identical.

### Scope boundary — what was deliberately left hand-maintained
The generator owns the JSON-LD block. The `<meta name="description">`, `<meta property="og:*">`, `<title>`, and `<link rel="canonical">` stay hand-maintained (they currently match the generated JSON-LD: `url`==canonical, `image`==`og:image`, `description`==meta description, verified 4/4). A future iteration could have the script sync those tags from `config.seo` too, so one field flows to meta + og + JSON-LD; deferred to keep this diff focused and to avoid fragile per-tag content-attribute regex on a runtime-shipped file without a browser smoke-test. Note `og:description` is intentionally a *shorter* string than the meta description (different social-card copy), so fully converging the description would take two config fields, not one. **✅ Closed in iteration 16** — the prerender script now generates the entire meta cluster (title/description/canonical/og:*/twitter:*) from `config.seo` + a new `seo.ogDescription` field for the shorter social-card copy; see iteration 16.

### Validation
- `node --check scripts/prerender-project-pages.js`, `shared.js`, `home.js`, `project-instance-loader.js` — **pass**; all four project configs + `data/taxonomy.json` — valid JSON.
- **Value preservation:** extracted each generated `<script type="application/ld+json">`, `json.load`'d it, asserted `headline`/`description`/`image`/`keywords`/`url` equal the pre-change hand-maintained values — **4/4 identical**; plus `@context`, `@type` Article, `mainEntityOfPage`, `author`, `publisher` structurally correct.
- **Cross-tag consistency (served):** generated `url` == `<link rel="canonical">`, `image` == `og:image`, `description` == `<meta name="description">` — **4/4**.
- **Idempotency:** md5'd all four pages, re-ran the script, md5'd again → **byte-identical (4/4)**.
- Served locally (`python3 -m http.server 8987`): all four pages + `index.html` → **200**; each served page has exactly one `ld+json` block, the `BEGIN/END project-jsonld` markers (1/1), and **0** occurrences of the removed "JSON-LD structured data:" comment. `index.html` **unchanged** (it has its own `Person` JSON-LD and is not processed by the script). Server confirmed stopped (`ss -ltn` → no listener).
- Caveat: the generated JSON-LD is static HTML (not runtime-built), so its correctness is fully verified here; the only deploy-time confidence step is Google's Rich Results Test against the live `maxlyj.com` URLs (same as iteration 9). The runtime render is provably untouched (the loader is unchanged; only `<head>` static content + configs changed, and the configs gain an inert key).

---

## ✅ Fixed in iteration 16 (SEO/maintainability: `<head>` meta cluster generated from config — closing the meta↔JSON-LD drift surface)

Iteration 15 single-sourced the JSON-LD from each config's `seo` block but deliberately left the `<title>`, `<meta name="description">`, `<link rel="canonical">`, `og:*`, and `twitter:*` tags hand-maintained (its scope-boundary note flagged them as the residual drift surface). This iteration closes that surface: the prerender script now generates the entire `<head>` SEO meta cluster from the **same** `config.seo` source the JSON-LD reads, so the meta tags, the Open Graph / Twitter cards, and the structured data can no longer drift apart.

### Audit — every meta tag is derivable from `config.seo` + the filename
Checked how each hand-maintained meta tag relates to `config.seo` (you can only generate it if the value can live there or be derived):
- **`<title>` / `og:title`** = `seo.headline + " | Yujia Max Liu"` — verified 4/4 (the suffix is a constant shared by all four pages).
- **`<meta name="description">`** = `seo.description` (== JSON-LD `description`, verified 4/4).
- **`og:description`** = a deliberately *shorter* social-card string for **all four** pages (farcry drops the trailing *"A Ubisoft Shanghai technical-art case study."*; division-2's differs in wording too) — so it needs its **own** config field, not `seo.description`.
- **`og:image` / `twitter:image`** = `seo.image` (== JSON-LD `image`, verified 4/4). Generating both from one source means the two hand-maintained URL copies can never diverge — the same defect class as the iteration-8 `HighResGameImage02jpg` typo (one value, two copies, one goes stale).
- **`canonical` / `og:url`** = site base (from `CNAME`) + `"/" + filename` (== JSON-LD `url`, already derived in iteration 15).
- **`og:type` = `"article"`, `twitter:card` = `"summary_large_image"`** — constants shared by all four, so they live in the script, not per-config.

### Architecture
- **Added `seo.ogDescription`** to each of the four configs, populated verbatim from the page's current hand-maintained `og:description` — so the shorter social-card copy is single-sourced rather than derived/truncated. (Runtime consumers access config by named fields only, so the extra key is inert at runtime — same property that made the iteration-15 `seo` block safe to add.)
- **Extended `scripts/prerender-project-pages.js`** to generate a `project-meta` cluster (BEGIN/END markers, same proven idempotent model as the `<noscript>` and JSON-LD blocks): `<title>`, `description`, `canonical`, `og:title/description/url/type/image`, `twitter:card/image`, all from `config.seo` + the derived canonical, HTML-escaped. The script strips any prior generated cluster **and** each hand-maintained tag it replaces (per-tag, property-anchored regexes that assert ≤ 1 match per tag and throw *before writing any file* if a page carries duplicates), then inserts the cluster immediately after the viewport `<meta>`, ahead of the JSON-LD block.

### Correctness notes
- **Proper HTML escaping is a side-improvement.** The hand-maintained `<meta name="description">` carried a bare `&` ("road & river"); the generator HTML-escapes it to `road &amp; river` (correct for an HTML attribute). The JSON-LD `description` keeps the bare `&` (correct for JSON inside `<script>`). Value preservation was validated by *un*escaping the generated attribute before comparing to the baseline.
- **The strip consumer is `[ \t]*\r?\n`, NOT `\s*`.** The JSON-LD block sits immediately after the meta cluster. A greedy `\s*` trailing consumer would eat the JSON-LD comment's leading 4-space indent, making the `JSONLD_BEGIN` anchor (which includes those spaces) fail to match on the next run and leaking a **duplicate JSON-LD block**. The bounded consumer eats only the newline after the meta END marker, leaving the JSON-LD block's indentation intact — a real interaction between two adjacent marker blocks that the single-block iterations didn't surface.

### Validation
- `node --check scripts/prerender-project-pages.js`, `shared.js`, `home.js`, `project-instance-loader.js` — **pass**; all four configs + `data/taxonomy.json` — valid JSON.
- **Value preservation:** for each of the 4 pages × 10 tags, the generated attribute (unescaped) equals the pre-change hand-maintained value — **40/40 identical**.
- **Cross-tag consistency:** generated `canonical` == `og:url` == JSON-LD `url`; `og:image` == `twitter:image` == JSON-LD `image` — **4/4**.
- **No duplicates / no leftovers:** exactly one `project-meta` cluster and one `project-jsonld` block per page (in order: meta → jsonld → preconnect); JSON-LD still parses as valid `Article`; the legacy "JSON-LD structured data:" comment is gone (**0/4**).
- **Idempotency:** md5'd all four pages, re-ran the script, md5'd again → **byte-identical (4/4)**.
- `git status`: exactly the 4 configs + 4 project HTML + the script changed; `index.html` **untouched** (it is not processed by the script and has its own `Person` JSON-LD).
- Served locally (`python3 -m http.server 8991`): all four pages → **200**, each carrying exactly one `project-meta` and one `project-jsonld` marker. Server confirmed stopped (`ss -ltn`).
- Caveat: the meta cluster is static `<head>` HTML (not runtime-built), so its correctness is fully verified here; the only deploy-time confidence step is Google's Rich Results Test / a social-card previewer against the live `maxlyj.com` URLs. The runtime render is provably untouched (the loader and `home.js` are unchanged; only `<head>` static content + an inert config key changed).

---

## ✅ Fixed in iteration 17 (performance: cut wasteful featured-carousel image preloading — P3 #13)

The featured carousel's preloading strategy warmed far more than the LCP needed, competing with first paint on slow connections. Two distinct defects, both in `home.js`, fixed together as one cohesive "stop fetching images you don't need yet" change. Chosen over the iteration-16 candidates per the objective's priority order (performance before content/architecture): P4 #17 (contact CTA) is **blocked** — no email exists anywhere in the repo and fabricating one is out of scope — and P4 #16 (skill-tag/taxonomy cross-check) is mostly documentation since the two lists are genuinely *different dimensions* (taxonomy = competency areas for filtering the works gallery; About `.skill-tag`s = concrete tools/engines); P3 #13 is a concrete, verifiable performance code change.

### Performance — neighbor warming reduced to cover-only (P3 #13)
- **`warmNeighborPages()` → `preloadPageAssets()` previously fetched the cover + every thumbnail + every HighRes "full" for *both* adjacent carousel slides on every `renderPage()`** — up to ~18 speculative image requests (1 cover + up to 4 thumbs + up to 4 HighRes fulls, × 2 neighbors), several of them large HighRes assets shown only on hover. That is the LCP contention P3 #13 flagged. `preloadPageAssets()` now warms **only the cover**: the cover is the slide's LCP image and the only asset shown the instant you navigate to a neighbor, so warming it alone preserves the "prev/next feels instant" benefit while dropping ~16 speculative fetches. When a neighbor becomes the current slide its thumbnails (small LowRes) render and fetch on demand, and the HighRes full swaps in on hover exactly as it already did for the current slide — the current slide's fulls were never preloaded (only neighbor fulls were), so this makes neighbors *consistent* with the current slide rather than regressing anything.

### Code quality — removed two redundant same-URL preloads (new finding)
- **`renderPage()` called `preloadImage(page.cover)` immediately after `frMainImage.src = page.cover`, and `preloadImage(thumbPath)` immediately after `thumbImage.src = thumbPath` inside the thumbnail render loop.** Both duplicated a fetch the visible DOM `<img>` was already making — setting `.src` on a real `<img>` starts the request, so a `new Image()` with the same URL is a redundant second request the browser then has to dedup. Removed both, leaving a brief comment at each site explaining *why* no explicit preload is needed (so a future contributor doesn't "restore" them for performance — same anti-re-introduction rationale as the iteration-10 dead-code deletions). `preloadImage` is now called exactly once (the neighbor-cover warm), so no helper was orphaned.

### Net effect
On initial homepage load the carousel now speculatively fetches **2 neighbor covers** instead of up to ~18 images (cover + thumbs + fulls × 2 neighbors) plus up to 5 redundant same-URL duplicates (1 cover + up to 4 thumbs on the current slide). Carousel navigation still feels instant (neighbor covers are warm); thumbnails/fulls fetch on demand when actually viewed. **Zero functional behavior changed** — only *which* images are requested ahead of time.

### Validation
- `node --check home.js / shared.js / project-instance-loader.js` — **pass**.
- Served locally (`python3 -m http.server 8971`): `/home.js` → **200**; served-byte assertions: `page.(thumbs|fulls).forEach(preloadImage)` → **0** (the surviving `page.thumbs.forEach` at ~line 470 is the thumbnail *render* loop, not a preload); `function preloadImage` defined → **1**; `preloadImage(` call sites → **1** (neighbor cover only); `preloadImage(page.cover)` present → **1**. Server confirmed stopped (`ss -ltn` → no listener).
- No-dead-code check: `preloadImage` / `preloadPageAssets` / `warmNeighborPages` / `preloadCache` all still referenced (`preloadImage` defined once, called once; the other three unchanged in role).
- Caveat: the *count* of fetched images is a runtime/network behavior; verified here by `node --check` + serve+grep + code reading. The change is provably safe because removing speculative/redundant image requests cannot break rendering — every image still loads when it is actually displayed (cover via the visible `<img>`, thumbs via the rendered thumbnail `<img>`s, fulls on hover). A manual load confirming the carousel still navigates and thumbs still swap the hero is the remaining confidence step; the LCP/network delta is a deploy-time Lighthouse measurement against `maxlyj.com`.

---

## ✅ Fixed in iteration 18 (content correctness + a dedicated content review)

The iteration-17 plan offered three browser-free candidates (P4 #16 skills↔taxonomy, a project-copy pass, P4 #15 naming policy). This iteration took the **content** axis — the objective's CONTENT REVIEW pillar had no dedicated artifact (only this CODE-REVIEW.md existed) and two shipping content defects surfaced on audit. So: fixed the two clear defects, opened a [`CONTENT-REVIEW.md`](./CONTENT-REVIEW.md) for the rest, and resolved P4 #16 as a documented non-defect.

### Correctness — d-walker missing-space typo
- **`d-walker-vs-sahelanthropus.json` → `projectDetails.blocks[1].text`** opened `"Ten years ago(2015), I experienced…"` — missing space before the year. It is the page's only body paragraph and its emotional hook, so the typo was the first thing a reader saw. Corrected to `"Ten years ago (2015), I experienced…"`. Verified gone from config **and** the pre-rendered `<noscript>` (the build-time pre-render inlines `block.text`, so re-running `scripts/prerender-project-pages.js` was required to keep the static↔runtime single-sourcing intact — the exact drift surface iterations 12–16 worked to eliminate).

### IA / copywriting — division-2 placeholder "Header" heading removed
- **`division2-tools.json`** carried an `h3` whose `text` was literally `"Header"`, above the banner image `Header_TheDivision2.jpg`. It was the **only** non-descriptive heading in the file — the other seven (`Procedural Level Generation`, `Level Streaming & Optimization`, `Boss Mechanics`, `Oil Truck Encounter`, `In-Game Shaders`, `LED Screen Setup`, `UI Programming & VFX`) each name a work area and carry a paragraph; `"Header"` had no paragraph and headed only a banner. Removed the `h3` block (the banner now follows the intro `h2`+`p` directly — a conventional hero lead-in that needs no heading); the banner image is preserved. Chose removal over inventing a heading — I cannot see the image, and a fabricated heading name is worse than "a banner needs no H3." Verified `>Header<` gone from served `/division-2.html` and the banner still present.

### New doc — `reviews/CONTENT-REVIEW.md`
- A dedicated content pass (the objective explicitly names this file). Covers per-project content assessment (Far Cry 6 = gold standard; Division 2 = strong; d-walker/raiden = thin), the full P4 #15 naming-inconsistency table with a recommended 4-surface policy, P4 #16 resolved as a non-defect (skills and taxonomy are genuinely different dimensions — tools/engines vs competency areas — with only Python/Houdini overlapping and no contradiction), the R2-side filename typos (`BossMachanic*`, `BossMachanic_Firearea*`, `CenimaticShader1` — documented for R2 remediation, URLs left intact), and a prioritized content backlog.

### Validation
- All 4 project configs + `data/taxonomy.json` — valid JSON; `node --check shared.js / home.js / project-instance-loader.js / scripts/prerender-project-pages.js` — **pass**.
- `scripts/prerender-project-pages.js` re-run: only the two edited pages changed (farcry/raiden untouched); **idempotent** (byte-identical second run).
- Served locally (`python3 -m http.server 8318`): `/`, `/division-2.html`, `/d-walker-vs-sahelanthropus.html` → **200**; served d-walker has `ago (2015)` (1) / `ago(2015)` (0); served division-2 has `>Header<` (0) and the banner image still present (≥1). Server confirmed stopped (`ss -ltn` → 0 listeners).
- Caveat: both fixes are data-level (`createDetailBlockElement` renders each block independently by type — no TOC/numbering, verified), so the rendered page changes are unambiguous; a manual load confirming d-walker's paragraph spacing and division-2's banner sits cleanly under the intro is the remaining confidence step.

---

## ✅ Fixed in iteration 19 (hygiene + semantic HTML: stray deploy annotation, conforming `<time>` dates)

A fresh audit pass (the explicit backlog is now mostly owner-gated or browser-gated) surfaced two concrete, static-HTML defects — one repo-hygiene, one HTML-validity — both fully verifiable without a browser. No JS changed this iteration.

### Hygiene — removed a stray deployment-redeploy annotation from `index.html`
- **`<!-- Retry deployment after transient GitHub Pages 502 annotation -->`** sat in the homepage `<head>` (introduced in commit `23aa70d` "Trigger Pages redeploy after transient deployment outage"). It was a process workaround — a commit made purely to nudge GitHub Pages into redeploying after a transient 502 — and the comment was the leftover footprint shipping to production HTML. Same defect class as the iteration-1 junk file (`udo apt install openssh-server`) and the iteration-3 `project-instance-test` artifact: deployment/process cruft that reached a live page. A site-wide grep confirmed it was the only such annotation (`Retry deployment` / `transient` appears in no other shipped file). Removed; it is an HTML comment in `<head>` so it never affected rendering or parsing — **zero behavior change**.

### Accessibility / semantic HTML — resume `<time>` elements are now conforming (P2 #10, tightened)
- The iteration-7 review of P2 #10 called the resume `<time>` elements "acceptable." An honest re-read shows they were actually **non-conforming**: six resume `<time>` elements (`Nov 2024 – Present`, `Dec 2021 – Nov 2024`, `Sep 2020 – Dec 2021`, `Mar 2019 - Sep 2019`, `Aug 2018 – May 2020`, `Sep 2014 – Jun 2018`) carried **date-range text and no `datetime` attribute**. Per the HTML spec, a `<time>` element without a `datetime` attribute must itself contain a valid machine-readable date/time string — and "Nov 2024 – Present" (a range with a word) is not one, so all six were invalid `<time>` usage. (Only the tag-system `<time datetime="2026-06-26">` was conforming.)
- **Fix:** added a `datetime` for each range's **start month** — `2024-11`, `2021-12`, `2020-09`, `2019-03`, `2018-08`, `2014-09`. The `datetime` attribute is the machine-readable value a `<time>` element is supposed to carry; the visible text keeps the full range unchanged, so there is **zero visual change**. The start month is the most useful machine signal for a resume entry ("this role began…"), and for the ongoing role reads naturally as "since Nov 2024." A date *range* cannot be a single `datetime` (the attribute takes one date or a duration, not a `start/end` pair), so the start-date approach is the minimal conforming fix that preserves the design; a fuller restructure (two `<time>` per range) is a larger change left to the owner.
- Verified: every `<time>` on the homepage now carries a `datetime` (7/7); **0** non-conforming `<time>` remain site-wide.

### Observed, not fixed (browser-gated) — nested interactive content in the featured carousel
- While re-reading the carousel I noted the thumbnail **`<button>`s are descendants of the `<a class="fr-card">` link** (rendered into `#fr-thumbs`, which sits inside the card `<a>`). Interactive content nested inside interactive content is an a11y/semantic smell. It is *not* an HTML-parser error here — the buttons are inserted via `createElement` + `appendChild`, and the DOM API permits any nesting (the "interactive content cannot nest" rule is enforced by the HTML *parser*, not by `appendChild`), so the DOM really is `a > button`. But the nested semantics overlap (the button and the link are both focusable and both navigate to `page.targetUrl`), which can confuse AT focus order. The behavior is intentional (hover/focus swaps the hero to the HighRes variant; click navigates — same target as the card), and fixing it is a **structural** change to the runtime carousel (e.g. make the card a non-link region and put navigation on specific child elements) that should be smoke-tested in a browser before committing. **Filed for a future browser-checked pass**, not changed this iteration.

### Validation
- `node --check shared.js / home.js / project-instance-loader.js / scripts/prerender-project-pages.js` — **pass** (no JS changed; sanity).
- Served locally (`python3 -m http.server 8319`): `/` → **200**; served bytes carry **0** `Retry deployment after transient` occurrences; **0** `<time>` elements without a `datetime`; the six resume `<time>`s carry `datetime="2024-11|2021-12|2020-09|2019-03|2018-08|2014-09"`. Server confirmed stopped (`ss -ltn` → no listener).
- Caveat: both fixes are static `<head>`/`<body>` HTML with no runtime/JS involvement, so correctness is fully verified here. The only residual confidence step is an AT pass confirming the resume dates still announce naturally with the added `datetime` (most screen readers read the text content, so this is expected to be a no-op audibly).

---

## ✅ Fixed in iteration 20 (site-completeness: favicon + custom 404)

The explicit backlog was depleted to owner-driven / browser-gated items, so this was another fresh-audit pass — this time over **site-completeness / IA** surfaces no prior iteration had touched. It surfaced two concrete, static-asset gaps, both fully verifiable without a browser and both shippable.

### Favicon — the site had none (browsers auto-requested `/favicon.ico` → 404)
- A repo-wide grep confirmed **zero** `<link rel="icon">` / `rel="apple-touch-icon"` / `favicon` references anywhere, and **no favicon asset**. Every browser automatically requests `/favicon.ico` on every page load; with no file, every request 404'd (a wasted round-trip + a generic broken-icon tab). For a branded portfolio this is a real polish/UX gap.
- **Fix:** derived two standard favicon artifacts from the existing brand mark (`images/logo.png` — the centered "MAX" shield/arrow) with a one-off Pillow script: (1) **`favicon.ico`** at the repo root, a multi-resolution icon (16/32/48) — this both satisfies the browser's automatic `/favicon.ico` request *and* serves as the default icon; (2) **`images/apple-touch-icon.png`** (180×180, opaque white bg) for iOS home-screen bookmarks. The source is wider than tall (119×60), so the script auto-trims the white margins to the content bounding box and pads to a centered square (no stretch, nothing cropped).
- **Wired `<link rel="icon">` + `<link rel="apple-touch-icon">` into all 5 shipped pages** (`index.html` + the 4 project pages). **Placement was chosen for prerender idempotency:** the links sit immediately after `<meta charset>`, *before* the viewport `<meta>`. The prerender script re-inserts its generated `<head>` clusters at two anchors — immediately *after* the viewport `<meta>` (the meta cluster) and *before* the Google Fonts preconnect (the JSON-LD). Placing the favicon links *after* the viewport would let the meta-cluster re-insertion push them down the head on every re-run (a non-byte-stable diff); placing them *before* the viewport puts them in the `html.slice(0, vpEnd)` portion the script never touches. Verified: re-ran `scripts/prerender-project-pages.js` and the links survived at exactly 2 per page, in position, with **no** other project-page drift (`git diff` = +2 lines per page, favicon only).

### Custom `404.html` — GitHub Pages was serving its default 404
- No `404.html` existed at the repo root, so any unmatched URL (`/typos`, stale links, etc.) fell through to GitHub Pages' generic white 404 — off-brand and no way back to the site. GitHub Pages automatically serves a root `404.html` for unmatched paths, so adding one is a zero-config improvement.
- **Fix:** added a self-contained, on-brand `404.html` — dark palette using the site's design tokens, a gradient "404" headline, a one-line explanation, and a prominent "Return to homepage" CTA (`href="/"`), with `:focus-visible` and `prefers-reduced-motion` handling. It carries `<meta name="robots" content="noindex">` so error pages aren't indexed, and the favicon links (absolute) so the 404 tab is still branded.
- **Deliberately self-contained** (inline `<style>` with the 5 token colors inlined, *not* `<link rel="stylesheet" href="/style.css">`): GitHub Pages serves `404.html` from arbitrary unmatched URL depths (e.g. `/foo/bar/`), where a *relative* `style.css` would resolve to `/foo/bar/style.css` and break; an *absolute* `/style.css` would work but couples the error page to the main stylesheet's variable names and pulls the full site CSS for a one-off page. Inlining is the robust, lightweight choice — documented in a banner comment so a future editor doesn't "fix" it by linking the stylesheet.

### Validation
- `node --check` on `shared.js` / `home.js` / `project-instance-loader.js` / `scripts/prerender-project-pages.js` — **pass** (no JS logic changed; sanity).
- Prerender idempotency: re-ran the script; `git diff --stat` = 5 pages × +2 lines (favicon links only), no unintended drift in the generated meta/JSON-LD/noscript clusters.
- Served locally (`python3 -m http.server 8765`): `/favicon.ico` → **200** `image/vnd.microsoft.icon`; `/images/apple-touch-icon.png` → **200** `image/png`; `/404.html` → **200** `text/html` (title, `noindex`, and "Return to homepage" CTA all present); `/` → **200** with the two favicon `<link>`s in the served `<head>`. Server confirmed stopped (`ss -ltn` → no listener).
- Caveat: favicon correctness is fully verified here (the asset exists, has the right MIME, is referenced from every page, and survives the build). The one residual confidence step is a visual check that the 119×60 source upscaled to 180×180 reads crisply on an iOS home screen — the source resolution is the ceiling (no higher-res logo exists in the repo).

---

## ✅ Fixed in iteration 21 (prefers-reduced-motion: CSS + JS)

The explicit backlog was depleted to owner-driven / browser-gated items, so this was another fresh-audit pass — this time over **motion accessibility**, a surface no prior iteration had audited. It surfaced one concrete, fully verifiable gap: the site has **21 `transition`/`transform` rules and 3 JS-driven smooth-scrolls** but honored **no** `prefers-reduced-motion` preference (verified `grep prefers-reduced-motion style.css` → 0). For a portfolio with an animated image-compare slider, carousel, sidebar overlay, and hover lifts, ignoring the OS "reduce motion" setting is a real vestibular-accessibility gap (aligns with WCAG 2.1 SC 2.3.3 Animation from Interactions) — the kind of thing a senior reviewer flags because it's commonly missed and trivially fixable.

A complete fix needs **both** halves, because CSS and JS motion are independent:
- **CSS cannot reach programmatic scrolls.** Three call sites pass `{behavior:"smooth"}` to `scrollIntoView` — `home.js` tag-click → works section, `home.js` sidebar/hash-link → target section, `project-instance-loader.js` active gallery thumb. No CSS rule can slow or stop these.
- **JS cannot cheaply enumerate the 21 declarative transitions** (slider width/transform, overlay opacity, card hover lift, sidebar max-width). A CSS media query is the right tool.

### Changes
- **Added a single-source-of-truth `prefersReducedMotion()` helper to `shared.js`** (loaded before every entry script, per the iteration-6 shared-global pattern). Returns `window.matchMedia("(prefers-reduced-motion: reduce)").matches`, with a **conservative fallback** — `false` when `matchMedia` is unavailable, so older browsers keep the existing smooth-scroll default rather than being silently forced to instant.
- **Gated all 3 JS smooth-scrolls** on the helper: each now passes `behavior: prefersReducedMotion() ? "auto" : "smooth"`. Final scroll position/offset logic is untouched (the `scroll-margin-top` / `scroll-padding-top` offset handling still applies); only the animated easing is removed for reduced-motion users.
- **Added a site-wide `@media (prefers-reduced-motion: reduce)` block to `style.css`** (universal selector, `!important`): `animation-duration`/`transition-duration` → `0.01ms`, `animation-iteration-count: 1`, `scroll-behavior: auto`. **Crucially this removes only the *motion*, not the *final state*** — the image-compare slider still snaps to the correct position, overlays still show/hide, cards still lift on hover. Durations use `0.01ms` (not `0`) so any `transitionend`/`animationend` listener still fires near-instantly — verified **none exist** (`grep transitionend|animationend` → 0 in both entry scripts), but `0.01ms` is the safe community idiom and costs nothing.

### Validation
- `node --check shared.js / home.js / project-instance-loader.js` — **pass**.
- **0 unguarded `behavior:"smooth"`** remain (`grep 'behavior:\s*"smooth"'` → the only hit is inside the `shared.js` comment describing the call sites); all 3 are now the ternary guard.
- `prefersReducedMotion` **defined exactly once** (`shared.js:39`) and **used at exactly 3 sites** (`home.js:119`, `home.js:264`, `project-instance-loader.js:399`); **0 redeclarations** in the entry scripts (no "Identifier already declared" collision).
- `style.css` braces balanced (231/231); the media query appears exactly once.
- Served locally (`python3 -m http.server 8765`): confirmed `shared.js` loads immediately before the entry script on all 5 pages, the helper ships in served `shared.js`, the 3 ternary guards ship in the served entry scripts, and the reduced-motion block ships in served `style.css`. Server confirmed stopped (`ss -ltn` → no listener).
- Caveat (same as prior runtime-touching iterations): the rendered *behavior* under the media query can only be confirmed by toggling the OS "reduce motion" setting in a browser and clicking a tag / sidebar link / gallery thumb. The static guarantees are airtight (well-specified media query + matchMedia; no transitionend reliance; final states preserved), so residual uncertainty is only "does it look right," not "does it work."

### Noted (not done this iteration) — `theme-color`
The same audit found the site ships **no `<meta name="theme-color">`** on any page. This is a separate, minor mobile-polish concern (colors the Android-Chrome / Safari mobile browser chrome and the iOS status-bar area) — on-brand and zero-risk to add (`content="--bg"`), but a *different* concern from motion accessibility, so it's left as a clean one-line-per-page candidate for a future iteration rather than muddying this unit. **✅ Closed in iteration 22** — added `<meta name="theme-color" content="#0f111a">` plus a sibling `<meta name="color-scheme" content="dark">` (and a canonical `color-scheme: dark` in `:root`) to all 6 pages; see iteration 22.

---

## ✅ Fixed in iteration 22 (mobile polish + dark-mode signaling: `theme-color` + `color-scheme`)

The explicit backlog was depleted to owner-driven / browser-gated items, so this was another fresh-audit pass — over **browser-chrome theming and dark-mode signaling**, the last unimplemented standard `<head>` surface (iteration 21 noted `theme-color` as a clean candidate and flagged `color-scheme` as a productive fresh-audit angle in its learnings). A repo-wide grep confirmed the site shipped **no `<meta name="theme-color">` and no `<meta name="color-scheme">` on any page** (and `style.css` declared neither), despite being an unconditionally dark site with no `prefers-color-scheme` media query. Both gaps are static HTML, fully verifiable without a browser, on-brand, and zero-risk — so they were fixed together as one cohesive "tell the browser this is a dark site" unit.

### Why both matter (they solve different problems)
- **`<meta name="theme-color" content="#0f111a">`** colors the mobile browser chrome — the Android-Chrome address bar and the Safari-mobile browser-chrome / status-bar area — to match the page background. On a dark portfolio the default white browser chrome above a dark page is a visible polish gap; `theme-color` makes the chrome blend into the site. The value is the page background token `--bg` (`#0f111a`), identical on all pages.
- **`<meta name="color-scheme" content="dark">`** is the more substantive of the two: it tells the browser the page is authored for dark mode, so it renders UA-controlled surfaces — **scrollbars, native form controls, `accent-color`, and the pre-CSS-load canvas background** — in their dark variants. Without it a dark site shows light scrollbars (a real visual inconsistency on long pages) and a brief **white flash** before `style.css` applies the dark background (the browser paints its default white canvas until CSS loads). Declaring `color-scheme: dark` makes the browser paint a dark canvas from the very first frame and theme scrollbars dark. This is a genuine UX/correctness improvement, not just mobile chrome.
- They are complementary, not redundant: `theme-color` is purely cosmetic chrome coloring; `color-scheme` governs UA rendering behavior. MDN best practice is to declare `color-scheme` in **both** the `<meta>` (parsed before CSS, governs the pre-CSS-load canvas + UA UI) and the CSS `:root` (the canonical declaration, applies once CSS is parsed) — both were added.

### Changes
- **Added `<meta name="theme-color" content="#0f111a">` and `<meta name="color-scheme" content="dark">` to all 6 shipped pages** (`index.html`, the 4 project pages, `404.html`). On the project pages they are placed **immediately before the viewport `<meta>`** (alongside the favicon links) — the same idempotent-safe zone discovered in iteration 20: `scripts/prerender-project-pages.js` re-inserts its generated `<head>` meta cluster immediately *after* the viewport `<meta>` (`html.slice(0, vpEnd) + metaHtml`), so anything *before* the viewport sits in the verbatim-copied slice and survives re-runs. Verified by re-running the prerender: all 4 project pages byte-identical (md5 before == md5 after), and `git diff --stat` shows only `+2` per project page with **zero** drift in the generated meta/JSON-LD/noscript clusters. On `404.html` (not processed by the prerender) the metas sit right after `<meta charset>` — the ideal early position for `color-scheme`, which should be resolved before first paint.
- **Added the canonical `color-scheme: dark;` declaration to `:root` in `style.css`** (with an explanatory comment) and to `404.html`'s self-contained inline `:root` block — complementing the `<meta>` per MDN's "declare it in both" guidance.

### Correctness notes
- **No light-theme conflict.** The site is unconditionally dark — `grep prefers-color-scheme style.css` → 0, and there is no light-mode CSS branch — so `color-scheme: dark` is correct and unambiguous (there is no light fallback to honor). This is distinct from sites that offer a theme toggle, where `color-scheme` would need `light dark`.
- **Same value everywhere.** All 6 pages (including the self-contained `404.html`, whose inline tokens reuse `--bg: #0f111a`) share one background, so one `theme-color` value applies universally — no per-page maintenance, and no drift surface.
- **Placement honors the iteration-16 adjacency rule.** The new metas are in the pre-viewport zone, which is *before* the prerender's meta cluster and JSON-LD block, so the bounded `[ \t]*\r?\n` strip consumers and the JSON-LD leading-indent anchors are unaffected (that interaction only bites between two *generated* marker blocks).

### Validation
- `node --check shared.js / home.js / project-instance-loader.js / scripts/prerender-project-pages.js` — **pass** (no JS logic changed; sanity).
- **Prerender idempotency:** md5'd all 4 project pages, re-ran `scripts/prerender-project-pages.js`, md5'd again → **byte-identical (4/4)**; `git diff --stat` = 5 pages × `+2` + `404.html` `+3` + `style.css` `+4`, **no** unintended drift in the generated meta/JSON-LD/noscript clusters.
- Served locally (`python3 -m http.server 8123`): all 6 pages → **200**; each carries exactly **1** `theme-color` and **1** `color-scheme`; served `style.css` carries `color-scheme: dark` (1); served `404.html` inline `:root` carries `color-scheme: dark` (1). Site-wide totals: **6/6** theme-color, **6/6** color-scheme. Project-page head order verified: the metas are on the lines **before** the viewport `<meta>` (farcry-6 lines 7–8 vs viewport line 9). Server confirmed stopped (`ss -ltn` → no `:8123` listener).
- Caveat: the visible chrome-color and scrollbar-darkening effects are browser-UA behaviors that can only be confirmed by loading the deployed `maxlyj.com` on a mobile browser (theme-color) and a desktop browser that respects `color-scheme` (scrollbars / pre-CSS canvas). The static guarantees are airtight (well-specified metas + a single canonical `color-scheme: dark`), and the change is provably inert for layout (it touches only UA-controlled surfaces, no site CSS selectors changed).

---

## ✅ Fixed in iteration 23 (accessibility: keyboard navigation broken on carousel thumbnails)

The explicit backlog was depleted to owner-gated / browser-gated items, so — per the iteration-22 learning — this was a **deeper runtime/correctness review** of the carousel interaction code. It surfaced one concrete, spec-confirmed, high-impact accessibility defect: **the featured-carousel thumbnails could not be activated by keyboard.** This is exactly the kind of bug a senior reviewer flags because it is invisible in a mouse-only click-through yet strands any keyboard / screen-reader user.

### The bug — `preventDefault()` on Enter/Space cancelled the native click
Each carousel thumbnail is a native `<button class="fr-thumb">` (`home.js`, `renderPage()`). It carried *two* activation paths:
- a `click` handler that swaps the hero preview AND navigates: `window.location.href = page.targetUrl;` (the carousel's primary action — go to the project), and
- a `keydown` handler that, on `Enter` or `" "`, called `event.preventDefault()` then `activateThumb()`.

The `keydown` handler was the bug. A native `<button>` fires its synthetic `click` event **as the default action of the keydown/keyup** for Enter/Space (Enter fires the click on `keydown`; Space fires it on `keyup`). `event.preventDefault()` on that keydown **cancels the default action, so the native `click` never fires.** Net effect: a keyboard user who focused a thumbnail and pressed Enter or Space got only the hero-preview swap (which the `focus` handler already performed) and **never navigated** — the thumbnail's primary action was unreachable by keyboard. A mouse click worked (click is a separate event unaffected by the keydown `preventDefault`), so the defect was invisible unless you navigated by keyboard. (Confirmed against the HTML activation model: *"For a native `<button>`, Enter triggers activation on keydown, Space on keyup; calling `preventDefault()` during the keydown cancels the native activation and thus the synthetic click."*)

### Internal proof it was a defect, not an intentional pattern
The **loader's own gallery thumbnails** (`.project-thumb`, `project-instance-loader.js` ~line 405–407) use a **click-only** pattern — no manual Enter/Space `keydown` handler at all — and keyboard users activate them fine (native Enter/Space → click → `renderProjectGallery`). That is the correct idiom for a native button. The carousel was the *only* place in the codebase that added the extra `keydown` + `preventDefault`, which can only *suppress* the native click the loader relies on. The divergence itself is the proof: click-only is sufficient for keyboard (the loader proves it); the carousel's extra handler could only add harm.

### Fix — removed the redundant + harmful keydown handler
Deleted the thumbnail `keydown` block entirely. Enter/Space now fall through to the native button activation → `click` → `activateThumb()` + navigate, restoring keyboard parity with the mouse. Nothing else changed: the `focus`/`blur` preview-swap, `mouseenter`/`mouseleave`, the carousel-wide ArrowLeft/ArrowRight slide navigation, and the mouse/touch click paths are all untouched. Added a comment at the site explaining *why* no Enter/Space handler exists (so a future contributor doesn't "restore" the broken pattern — same anti-re-introduction rationale as the iteration-10/17 deletions).

### Why this runtime change is safe without a browser smoke-test
Unlike the iteration-19 `a > button` nested-interactive finding (a *structural* DOM change that genuinely wants a browser check), this is a **logic fix that restores native semantics** — the same class of provably-safe change as iteration 17's preload-trimming:
- A native `<button type="button">` is *specified* to fire `click` on Enter/Space. Removing a JS handler that suppressed that cannot break activation; it can only restore it.
- The `click` handler is unchanged and covers every activation path (mouse, touch, Enter, Space).
- The only behavior delta is that keyboard Enter/Space now navigates (matching the mouse) instead of doing nothing — strictly an improvement, with no regression for any input method (verified: Space on a focused button does not scroll, so no page-scroll side effect returns).

### Validation
- `node --check` on `home.js` / `project-instance-loader.js` / `shared.js` / `scripts/prerender-project-pages.js` — **pass**.
- **Removed-pattern grep:** the harmful `event.key === "Enter" || event.key === " "` block → **0** in `home.js`; the explanatory comment → **1**.
- **Remaining keydown handlers in `home.js` are all legitimate** (not Enter/Space on a button): line 277 = sidebar Escape-to-close; line 561 = carousel ArrowLeft/ArrowRight slide nav. Remaining `preventDefault` calls (sidebar hash-link click, nav-button click, carousel arrows) are all unrelated.
- **Sibling-loader consistency:** `project-instance-loader.js` `.project-thumb` is click-only (no Enter/Space handler) — the carousel now matches it.
- Served locally (`python3 -m http.server 8726`): `/home.js` → **200**; served bytes carry the removed pattern **0**×, the comment **1**×, the thumbnail click+navigate handler **1**×, the focus preview-swap **1**×, and carousel arrow nav **1**×. Server confirmed stopped (`ss -ltn` → no `:8726` listener).
- No project page, `index.html`, config, or CSS changed → the prerender is unaffected and was not re-run.
- Caveat: the actual keyboard-activation → navigation behavior can only be exercised in a real browser (Tab to a thumbnail, press Enter). The static guarantees are airtight (native button activation is well-specified; the click handler covers navigation; the loader proves click-only works for keyboard), so residual uncertainty is only "confirm in browser," not "does it work."

---

## ✅ Fixed in iteration 24 (runtime review: version-tag external-API path + completing the `preventDefault` audit)

Continuing the **deeper runtime/correctness review** pivot that paid off in iteration 23 (the iteration-22 learning said the static-`<head>`/site-completeness surfaces are exhausted and the productive remaining angle is spec-grounded JS logic bugs invisible to a mouse click-through). Iteration 23's learning explicitly named two iter-24 candidates — *"the remaining `preventDefault`/event-ordering sites"* and *"the `loadVersionTag` GitHub-API failure path"* — and both were taken this iteration. Both are provably-safe logic fixes (no structural/DOM change), fully verifiable without a browser.

### Robustness / performance — `loadVersionTag` now caches the commit SHA per session
- **The defect:** `loadVersionTag()` (`shared.js`) populates the decorative `.top-bar-version` pill by fetching `https://api.github.com/repos/MaxLYJ/maxlyj.github.io/commits?…` — the GitHub REST API, **unauthenticated**, called once on the homepage (`home.js:11`, module load) **and again on every project page** (`project-instance-loader.js:632`, after the template injects the top bar). A visitor browsing the homepage + the 4 project pages therefore made **5 unauthenticated GitHub API calls per session**. GitHub's unauthenticated rate limit is **60 requests/hour per originating IP** (shared across all visitors behind the same NAT/corporate proxy/CGNAT), so this amplified a fragile external dependency 5× — and on every repeat call the pill sat empty until the (rate-limited or slow) round-trip resolved, with no cross-page memory that a *prior* page in the same session had already fetched the SHA.
- **The fix:** cache the resolved label in `sessionStorage` and reuse it across pages within the same browsing session. `loadVersionTag()` now reads `sessionStorage.getItem("maxlyj:version-tag")` first; on a hit it paints the pill synchronously and returns (no network). On a miss it fetches as before, and on success writes `sessionStorage.setItem(...)` so the next page in the session hits the cache. This collapses the per-session call count from **5 → 1**: the first page fetches, the other four read the cache and show the version instantly (and never leave the pill empty on a rate-limited repeat).
- **Why `sessionStorage`, not `localStorage`:** sessionStorage is scoped to the tab and cleared when the tab closes, so visitors reaching the site in a *new* session after a deploy always fetch the fresh SHA rather than reading a stale one persisted across days. The staleness window is at most one browsing session — exactly right for a decorative "current version" pill.
- **Conservative fallback (same pattern as `prefersReducedMotion`):** every `sessionStorage` access is wrapped in its own `try/catch` — `getItem` failure → `cached = null` (falls through to fetch); `setItem` failure → leave uncached (next page refetches). So an unavailable sessionStorage (Safari private mode, disabled storage, the `SecurityError` some embed contexts throw) silently degrades to the original always-fetch behavior. No failure path is *worse* than before; the cache is strictly additive.
- **Negative-cache intentionally NOT added:** a transient failure (rate limit, offline, GitHub 5xx) is not cached, so the next page retries rather than pinning a "could not load" state for the whole session. Positive-cache-only is the conservative, correct choice.

### Code quality — removed a redundant `preventDefault` + `stopPropagation` block (preventDefault audit, complete)
- Iteration 23's learning asked to audit *"the remaining `preventDefault`/event-ordering sites."* The full inventory:
  - `home.js:259` — sidebar in-page hash-link click; `preventDefault` fires only when a smooth-scroll target exists (else the native anchor jump is kept). **Legitimate.**
  - `home.js:559` / `:562` — carousel ArrowLeft/ArrowRight `keydown`; `preventDefault` stops horizontal page scroll while advancing the slide (APG carousel pattern). **Legitimate.**
  - `project-instance-loader.js:724` — image-compare slider keyboard (arrows/PageUp-Down/Home/End); `preventDefault` after a handled key (the `default:` returns early) stops page scroll during slider operation. **Legitimate.**
  - `project-instance-loader.js:738` — slider `mousedown`; `preventDefault` stops text/image selection during drag. **Legitimate.**
  - `home.js` carousel **prev/next nav buttons** — ❌ **redundant no-op, removed.** The code registered *two* `click` listeners on each `.fr-nav` button: one that called `event.preventDefault()` + `event.stopPropagation()`, and a second that called `movePage(±1)`. Both calls in the first listener were inert: `.fr-nav` is a `<button type="button">` (no default action to cancel, so `preventDefault` does nothing), and the buttons are **siblings** of the card `<a>` (not descendants) with **no ancestor `click` handler** (the `<section>` carries a `keydown` handler only), so `stopPropagation` had nothing to block. The split into two listeners was confusing (it implied a propagation/default-action problem that did not exist). Collapsed to a single `movePage` listener per button; the click → `movePage` path is unchanged.
- **Audit conclusion:** every `preventDefault`/event-ordering site in the codebase is now accounted for and either legitimate or removed. No iter-23-class activation bug (a handler suppressing a native click) remains — the only such instance was fixed in iteration 23.

### Why these runtime changes are safe without a browser smoke-test
Same "restores / preserves native semantics, no structural change" class as iterations 17 and 23:
- **Caching:** reading/writing `sessionStorage` (well-specified, wrapped in `try/catch` with a conservative fetch fallback) cannot break the existing fetch→paint path — it only short-circuits it on a cache hit. The fetched value and the cached value are byte-identical (the cache stores the exact `el.textContent` label written on success), so a cache hit reproduces the same display as a fresh fetch.
- **prev/next cleanup:** removing `preventDefault()` (a no-op on `type=button`) and `stopPropagation()` (no ancestor listener) from a `<button>`'s click cannot change behavior; the surviving `movePage` listener is unchanged and is the only activation path. Strictly a clarity improvement.

### Validation
- `node --check shared.js / home.js / project-instance-loader.js / scripts/prerender-project-pages.js` — **pass**.
- **Caching markers (served):** `shared.js` carries `VERSION_TAG_CACHE_KEY` **3**× (declaration + `getItem` + `setItem`) and `sessionStorage.getItem`/`setItem` **2**×; the cache-read short-circuit (`if (cached) { el.textContent = cached; return; }`) and the conservative `catch` fallbacks are present.
- **prev/next cleanup (served):** `home.js` has exactly **2** `addEventListener("click", () => movePage…)` (prev + next) and **0** `event.stopPropagation()` *calls* (the word survives only in the explanatory comment); the redundant two-listener `forEach` block is gone.
- **preventDefault audit (served):** actual `event.preventDefault()` calls in `home.js` are now exactly the 3 legitimate sites (sidebar hash-link, carousel ArrowLeft, carousel ArrowRight); the loader's 2 sites are unchanged and legitimate.
- Served locally (`python3 -m http.server 8727`): `/`, `/shared.js`, `/home.js`, `/project-instance-loader.js`, `/farcry-6.html` → all **200**. Server confirmed stopped (`ss -ltn` → no `:8727` listener).
- No project page, `index.html`, config, or CSS changed → the prerender is unaffected and was not re-run.
- Caveat: the cache hit/miss *behavior* and the sessionStorage persistence across page navigations can only be exercised in a real browser (DevTools → Application → Session Storage, then browse several pages and watch the Network panel collapse to one `api.github.com` request). The static guarantees are airtight (well-specified storage API, byte-identical cached value, conservative fetch fallback, no structural DOM change), so residual uncertainty is only "confirm in browser," not "does it work."

---

## 🔴 Remaining issues — prioritized for future iterations

### P1 — Correctness / robustness
1. **JS-rendered project pages are SEO-blind beyond meta tags.** `project-instance-loader.js` fetches `template-content.html` + a per-project JSON config and builds the entire DOM at runtime. Search engines see an empty `<body>` (only `<noscript>` + a mount div). **Addressed in iteration 9 (option b):** schema.org `Article` JSON-LD on all four pages (headline/description/cover/author/keywords in static HTML). **Addressed in iteration 12 (option a):** a build-time pre-render script (`scripts/prerender-project-pages.js`, run pre-deploy) now inlines each project's real content — title, description, cover, metadata, tags, and *every* project-details block — as semantic HTML inside a `<noscript>` block in the page `<body>`, so crawlers and no-JS users see real on-page text/images, not an empty mount. Chose `<noscript>` over primary visible body for safety (no browser/jsdom to smoke-test a hydration refactor): RAWTEXT parsing means it can never duplicate/flash/conflict with the runtime render. **Remaining (long-term, gated on a browser smoke-test):** a true SSR/hydration refactor that puts the content in the primary visible body (marginally higher SEO weight than `<noscript>`) and lets the loader reuse the static DOM instead of rebuilding; the same build pass could also generate the per-page JSON-LD from the config — **✅ done in iteration 15** (the four hand-maintained JSON-LD blocks are now generated from each config's `seo` block, deduped and `JSON.stringify`-validated) — leaving only the **publication-date gap** (configs carry only a work-period `time` string, so JSON-LD omits `datePublished` rather than fabricating one — a real data gap).
2. ~~**No `<html>` error handling when the template or config fetch fails.** `loadProjectInstanceTemplate()` silently `return`s on any failure, leaving a blank page with no user-facing message beyond `<noscript>`. Add a visible fallback/error state inside the mount point (e.g., “Project could not be loaded — return home”).~~ **✅ Fixed in iteration 3** (visible `role="alert"` error block with a "Return to homepage" CTA rendered into the mount point on every failure path — see below).
3. ~~**`project-instance-test.html` is a test artifact shipped to production.** It has `data-project-slug="project-instance-test"` and a config, is not in `sitemap.xml`, and is reachable by anyone. Remove from the live site (or move behind a non-published path). Also consider deleting `Resources/Project Instances/config/project-instance-test.json`.~~ **✅ Fixed in iteration 3** (both `project-instance-test.html` and its config `git rm`'d; the page now 404s).
4. **`canLoadProjectImage` / image-extension probing (`home.js`) issues a network request per extension per role** to discover `png → jpg → svg`. This is clever but slow and produces 404s in server logs. Prefer declaring the extension explicitly in the config, or ship a small manifest. **(Resolved in iteration 10 — deleted via option (a).)** The entire `if (projectTemplateMain)` block in `home.js` that contained this probing — plus `hydrateProjectTemplateImages` and a duplicated `initializeProjectGallery` — was confirmed dead in production (the trigger element exists only in `template-content.html`, which is not shipped and whose scripts never execute; `home.js` only loads on `index.html`, where the element is absent) and removed, along with its orphaned module-scope symbols. The extension-probing concern no longer exists in the codebase.
5. ~~**`home.js` calls `loadTaxonomyManifest()` twice on the homepage** — once at module scope (for the tag system) and again inside the featured-section IIFE via `loadFeaturedTaxonomy()`. Unlike the loader, `home.js`'s `loadTaxonomyManifest` is not memoized. Memoize / share the promise.~~ **✅ Fixed in iteration 2** (memoized single fetch; `loadFeaturedTaxonomy` removed).
24. **(Content correctness — identified iteration 11.) d-walker & raiden project pages ship template-scaffold placeholder gallery thumbnails.** Both configs set `images.thumb_01..04` to `Resources/Featured Recommendations/pages/fr-*/fr_*__thumb_0N.svg` — 476-byte scaffold SVGs that render the literal text *"Thumbnail 0N"* / *"fr_template-page__thumb_0N.svg"* on a colored rectangle (the sibling `fr_*__main.svg` files are the same scaffold, labeled *"Template Main Placeholder"*). These are the unreplaced output of the featured-recommendation scaffold (`Skills/featured-recommendation-page/SKILL.md` step 3: *"Replace placeholder images"*); the Far Cry 6 and Division 2 pages use real R2 `featured/LowResGameImage0N.jpg` thumbnails, but these two short-film projects were never populated. **No real thumbnail variants exist on R2** (probed `featured/LowResGameImage0N`, `thumb_0N.{jpg,png}` — all 404), so this requires real assets uploaded to R2 + the config repointed before it can be fixed. The covers are fine (d-walker `cover.webp`, raiden `cover.png` — both verified real artwork). The related raiden block-level placeholders (a `placehold.co` image and a `VIDEO_ID_PLACEHOLDER` video) were removed in iteration 11; the thumbnails were left (duplicating the cover 4× was rejected as reading-more-broken-than-placeholders) and documented here.

### P2 — Accessibility (deeper pass)
6. ~~**Carousel (`featured-recommendations`) is not a carousel widget for AT.** It lacks `role="region"`, an `aria-roledescription="carousel"`, `aria-live` for the changing title/description, and arrow-key support. The prev/next buttons work but the auto-changing main image on thumb hover has no programmatic state announcement. Needs a proper ARIA carousel pattern.~~ **✅ Fixed in iteration 2** (region + carousel role, polite live region, arrow-key nav, aria-controls; card kept as a link — see design note above).
7. ~~**The image-compare slider has no keyboard / screen-reader support** (`initImageCompareSliders` in `project-instance-loader.js`). It's mouse/touch only. Add a focusable range-style handle with `aria-valuenow/min/max` and arrow-key handling, plus an accessible name.~~ **✅ Fixed in iteration 5** (handle is now `role="slider"` `tabindex="0"` with `aria-valuemin/max/now/valuetext`, a label built from Before/After, and Arrow/PageUp-Down/Home/End handling; state unified across mouse/touch/keyboard; decorative chevrons `aria-hidden`; focus-visible ring added).
8. ~~**`aria-label="Tag system update reminder"` remains on a role-less `<div>`** (after removing `role="note"`). An `aria-label` on a generic `<div>` is ignored by AT — either drop the attribute or give the element an appropriate role. (Minor.)~~ **✅ Fixed in iteration 5** (attribute removed — the element's visible text + `<time>` are already accessible).
9. ~~**Color contrast**: verify `--muted: #b5bedf` on `--bg: #0f111a` and accent-on-dark combinations against WCAG AA for body text and small UI. Run an automated contrast audit (axe/Lighthouse).~~ **✅ Resolved in iteration 7** (full WCAG 2.1 contrast audit: every text-token/background pairing clears AA ≥4.5:1 — tightest real pairing is `--accent-2` on the chip background at 5.37:1; `--bg`-on-`--accent` button text is 7.93:1. No token changes needed).
10. ~~**`<time>` elements**: the tag-system `<time datetime="2026-06-26">` and resume `<time>`s are fine, but resume date ranges use an en-dash inside text nodes — acceptable; ensure they're not announced awkwardly.~~ **✅ Fixed in iteration 19.** The iteration-7 "acceptable" verdict was re-audited and found too lenient: the six resume `<time>` elements carried date-range text with **no `datetime` attribute**, making them non-conforming (a `<time>` without `datetime` must contain a valid machine-readable date; "Nov 2024 – Present" is not one). Added a `datetime` start-month to each (`2024-11` … `2014-09`); visible range text unchanged. All 7 homepage `<time>` are now conforming. See iteration 19.
25. ~~**No `prefers-reduced-motion` handling** — the site has 21 `transition`/`transform` rules (image-compare slider, sidebar overlay, carousel, card hover lifts) and 3 JS `scrollIntoView({behavior:"smooth"})` calls but honors no OS "reduce motion" preference. Affects users with vestibular sensitivity (aligns with WCAG 2.1 SC 2.3.3).~~ **✅ Fixed in iteration 21** — added a `prefersReducedMotion()` helper to `shared.js` (conservative fallback), gated all 3 JS smooth-scrolls on it (`behavior: prefersReducedMotion() ? "auto" : "smooth"`), and added a site-wide `@media (prefers-reduced-motion: reduce)` block to `style.css` that neutralizes transitions/animations via the universal selector (`0.01ms` idiom) without removing final states. See iteration 21.

### P3 — Performance
11. ~~**Render-blocking Google Fonts.** Both homepage and project pages load the full Inter stylesheet synchronously in `<head>`. Consider `media="print" onload="this.media='all'"` swap, or self-host + `font-display: swap` (already requested via `&display=swap`, but the CSS file itself is blocking).~~ **✅ Fixed in iteration 7** (Inter stylesheet now loads async via `media="print"` + `onload` swap on all 5 pages + template, with a `<noscript>` fallback; `style.css` left blocking as critical CSS).
12. ~~**No `width`/`height` on JS-generated card images** → potential CLS as cards hydrate. Consider fixed aspect-ratio containers in CSS (`.card img { aspect-ratio: 16/9 }`) so lazy-loaded images don't reflow.~~ **✅ Resolved in iteration 14** (re-scoped by an honest audit). The literal concern was already mitigated: `.card img` reserves its box via a **fixed `height: 230px`** (`210px` mobile), and the featured/project images reserve via `aspect-ratio: 16/9` — so no per-image CLS existed there. The genuine residual gaps the audit surfaced were the **brand logo** (no `width`/`height`, no CSS `aspect-ratio` → small horizontal snap on load) and the **LCP hero `#fr-main-image`** (no `fetchpriority="high"` — the iteration-1 LCP-priority treatment was applied to the portrait, not the hero). Both fixed: logo got its real `width="119" height="60"`; the hero got `fetchpriority="high"` + `width="1280" height="720"` (matches the CSS 16/9). Every homepage image now either has explicit dimensions or is reserved by CSS aspect-ratio/fixed-height; LCP element is high-priority.
13. ~~**`preloadImage` in the featured carousel warms neighbor pages aggressively** (cover + all thumbs + all fulls). On slow connections this competes with the LCP. Consider warming only the cover of neighbor pages.~~ **✅ Fixed in iteration 17.** `preloadPageAssets()` now warms **only the cover** of each neighbor slide (the carousel LCP), dropping up to ~16 speculative fetches (neighbor thumbs + HighRes fulls) that competed with first paint. Also removed two redundant same-URL preloads in `renderPage()` (the visible `<img>` `.src` already fetched the cover and each thumb). See iteration 17 above.
14. **Images are served from the R2 public bucket with no `width`/`resize` transform** — full-resolution assets are shipped and resized by the browser. Investigate Cloudflare Image Resizing or pre-generated LowRes/HighRes variants for cards (the `thumb_0N` / `full_0N` split already hints at this — extend it to the works gallery).

### P4 — Content / IA
15. **Inconsistent project naming across data sources.** Taxonomy titles (“Far Cry 6”, “Division 2”) vs. config titles (“Far Cry 6 — Procedural Generation”, “Division 2 Tools Showcase”) vs. resume headings (“Tom Clancy's The Division 2”). Decide on a canonical display-name policy (e.g., card = short name, detail page = full title) and document it. **Analyzed in iteration 18** — see [`CONTENT-REVIEW.md`](./CONTENT-REVIEW.md#p4-15--inconsistent-project-naming-across-surfaces-real-recommend-a-documented-policy) for the full 4-surface comparison table (Division 2 is the worst offender, appearing in 4 forms) and a recommended policy. The fix is a documented owner-driven naming decision, not a code change; deferred.
16. ~~**About skills list has no taxonomy backing.** The hardcoded `.skill-tag` chips in the About section are a separate, manually-maintained list from the tag system in `taxonomy.json`. Consider unifying or at least cross-checking.~~ **✅ Resolved in iteration 18** (documented as a non-defect). The two lists are genuinely *different dimensions* — About `.skill-tag`s = concrete tools/engines/languages (a "what I know" billboard); `taxonomy.json` tags = competency areas used to *filter* the works gallery, tuned to Senior-TA job-desk frequency. Exact overlap is only Python + Houdini, and **no skill-tag contradicts a taxonomy tag**; merging them would *reduce* signal. Full analysis in [`CONTENT-REVIEW.md`](./CONTENT-REVIEW.md#p4-16--about-skill-tags-vs-taxonomy-tags-not-a-defect--two-different-dimensions-verified-no-contradiction).
17. **Footer/section copy is thin.** No contact CTA, no resume download, no email. For a senior-TA portfolio, a clear contact path is expected.
18. ~~**`project-instance-loader.js` uses an apostrophe in a comment** and otherwise the code is clean, but the large block of legacy `initiative/pipeline/result` fallback (lines ~325–383) can be retired once all configs migrate to `projectDetails.blocks`. Track migration and delete dead code.~~ **✅ Fixed in iteration 10** (the ~60-line legacy `initiative`/`pipeline`/`result`/`placeholderImage`/`placeholderVideo` fallback in `renderProjectDetails` deleted — verified 0/4 configs use the legacy shape).

### P5 — Architecture / maintainability
19. **Duplicated logic between `home.js` and `project-instance-loader.js`**: `toCdnUrl`, `CDN_BASE`, `loadVersionTag`, gallery init, sidebar toggle were duplicated across the two files (image-compare init, it turned out, lives only in the loader). **Part 1 done in iteration 6:** the four byte-identical helpers (`CDN_BASE`, `toCdnUrl`, `loadVersionTag`, `TAXONOMY_MANIFEST_PATH`) are extracted into `shared.js` and single-sourced. **Part 2 (remaining):** the sidebar-toggle and gallery-init copies still duplicate but have real behavioral divergence (sidebar hash-smooth-scroll on the homepage only; gallery entry points differ) — merging them needs parameterization and a browser smoke-test.
20. ~~**`template-content.html` carries a full `<head>` that is never applied** to project pages (only `<body>` fragments are injected). The `<head>` is misleading dead weight — either strip it or actually use it.~~ **✅ Resolved in iteration 13** — removed the dead `<script src="home.js">` (DOMParser-created documents have scripting disabled, so parsed `<script>` tags never execute — the tag was a production no-op and misleadingly implied home.js drives the template) and added a banner comment documenting the file as a non-shipped fragment source (only 4 nodes extracted via fetch+DOMParser; `<head>` is local-preview-only; sidebar toggle wired by the loader's `initSidebarToggle()`). Chose **document + remove the dead script** over stripping the `<head>` so the file's parse behavior is unchanged without a browser smoke-test; the loader's four `querySelector`s still match the same four elements. (Minor leftover: `data-project-template-autohydrate` is now a vestigial attribute the loader sets but nothing reads — left as harmless, documented in AGENTS.md.)
21. ~~**The `Resources/Wix/` directory** (an entire saved Wix site incl. minified bundles, `*.js.download` files) is committed to the repo and only the logo PNG is referenced. Move the logo to a clean `images/` or `assets/` path and untrack the Wix archive — it bloats the repo and is brand-confusing.~~ **✅ Fixed in iteration 4** (13 MB / 72-file archive `git rm`'d; logo relocated to `images/logo.png` as a tracked rename; `Resources/Wix/` added to `.gitignore`).
22. **No linting/formatting/test harness** (per `AGENTS.md`). For a static site this is optional, but adding `eslint` + `prettier` + `htmlhint` + a link checker in CI would catch regressions like the `console.log` that shipped.
23. **`CNAME` = `maxlyj.com`, canonical/OG use `https://maxlyj.com/`**, but the GitHub repo is `MaxLYJ/maxlyj.github.io` and `home.js` hits `api.github.com/repos/MaxLYJ/...`. Confirm the custom domain + Pages setup is consistent and the R2 public bucket URL (`pub-dc4e1f00955f4568a77da06925201843.r2.dev`) is intended to be public/exposed (it currently is, in client code).

---

## Suggested iteration plan
- ~~**Iteration 2:** P1 #2 (visible load-error state) + P1 #5 (memoize taxonomy) + P2 #6 (ARIA carousel) — accessibility & robustness on the homepage hero.~~ — #5 and #6 done; #2 deferred.
- ~~**Iteration 3:** P1 #2 (visible load-error state on project pages) + P1 #3 (remove `project-instance-test` artifact) — robustness + repo hygiene.~~ — done.
- ~~**Iteration 4:** P5 #21 (untrack Wix archive, relocate logo) — repo bloat.~~ — done.
- ~~**Iteration 5:** Accessibility — P2 #7 (image-compare slider keyboard/AT support) + P2 #8 (redundant `aria-label` on role-less div).~~ — done.
- **Iteration 6:** ~~P5 #19 (extract `shared.js`)~~ — **part 1 done**: the four byte-identical helpers (`CDN_BASE`, `toCdnUrl`, `loadVersionTag`, `TAXONOMY_MANIFEST_PATH`) extracted to `shared.js`, loaded before the entry script on every page; also fixed the project-page version-pill bug the extraction surfaced.
- **Iteration 7:** ~~P3 #11 (non-render-blocking Inter font)~~ — **done** on all 5 pages + template (async `media="print"` swap + `<noscript>` fallback; `style.css` kept blocking as critical CSS). Also ran a full WCAG contrast audit (P2 #9 → resolved, all pairings pass AA) and re-scoped P1 #4 (image probing is dead in production — only runs if `template-content.html` is opened directly, which is not a shipped path).
- **Iteration 8:** ~~Next-planned was P5 #19 *part 2* (sidebar/gallery dedup), but a higher-priority correctness bug surfaced during review~~ — fixed a malformed R2 object URL (`full_02` missing the `.jpg` dot in `farcry6-procedural-generation.json`) that 404'd a live featured-recommendation image on the Far Cry 6 page. Prioritized under "critical bugs first" over the planned architecture work; also documented the `CenimaticShader1.png` misspelling as an R2-side fix (left the URL intact to avoid re-introducing the same 404 class).
- **Iteration 9:** ~~P5 #19 *part 2* (sidebar/gallery dedup) was the plan, but the higher-priority SEO item P1 #1 was addressed instead~~ — added schema.org `Article` JSON-LD to all four JS-rendered project pages so crawlers parse the project's real facts from static HTML instead of an empty body (option b of P1 #1). Documented the intentional `datePublished` omission (configs have no publication date) and updated P1 #1 to scope the remaining work to option (a) build-time static pre-render.
- **Iteration 10:** ~~P5 #19 *part 2* (sidebar/gallery dedup) was the plan, but it needs a browser smoke-test (the two sidebar copies genuinely diverge — homepage does in-page hash smooth-scroll + `history.replaceState`; the loader copy only open/close + mobile auto-close — so a naive merge would regress the homepage)~~ — instead did a **dead-code sweep** that was fully verifiable without a browser: deleted the dead legacy `projectDetails` fallback (P4 #18) and the dead `[data-project-template-folder]` template-hydration block in `home.js` (P1 #4, incl. the runtime image-extension probing), plus an orphaned comment; **~205 lines removed, zero shipped behavior changed.** P5 #19 part 2 remains deferred pending a browser/jsdom check.
- **Iteration 11:** ~~was planned as P1 #1 option (a) (static pre-render), but a higher-priority content-correctness defect surfaced during review~~ — discovered that **two of the four live project pages shipped template-scaffold placeholder media**, and per "critical bugs first" that precedes SEO work. Removed raiden's placeholder Project-Details blocks (a `placehold.co` image + a `VIDEO_ID_PLACEHOLDER` broken YouTube embed); verified d-walker/raiden covers and the d-walker video are real (no change); documented the remaining d-walker/raiden placeholder-thumbnail gap as **P1 #24** (needs real R2 assets — none exist to repoint to). The pre-render is deferred to iteration 12.
- **Iteration 12:** ~~P1 #1 option (a) — static pre-render of project pages~~ — **done.** Added `scripts/prerender-project-pages.js` (dependency-free Node, run pre-deploy) that inlines each project's real content (title, description, cover, metadata, tags, every detail block) as semantic HTML inside a `<noscript>` block on all four project pages, plus a `.project-noscript` style block. Chose `<noscript>` over primary visible body for safety (no browser to smoke-test a hydration refactor) — RAWTEXT parsing guarantees it can't duplicate/flash/conflict with the runtime render. Found + fixed a self-inflicted bug along the way (the script must mirror the loader's *raw* cover assignment, not apply `toCdnUrl`). Idempotent (re-run = byte-identical), fully validated without a browser.
- **Iteration 13:** ~~P5 #20 (strip or document the dead `<head>` in `template-content.html`)~~ — **done**: removed the dead `<script src="home.js">` (DOMParser-created documents have scripting disabled, so parsed scripts never execute — it was a production no-op and misleadingly implied home.js drives the template) and added a banner comment documenting the file as a fragment source (4 nodes extracted via fetch+DOMParser, `<head>` is local-preview-only, sidebar wired by the loader). Chose document+remove over stripping the `<head>` (no parse-behavior change without a browser smoke-test). Also corrected **AGENTS.md staleness** accumulated across iterations 6 & 10: it described deleted code (home.js template-hydration, the legacy `initiative/pipeline/result` fallback, the `fr-` folder + runtime image-extension probing) and a non-existent `featuredPages` array; rewrote the Dual-JS, Featured-Recommendations, and config-schema sections to match shipped code. Noted `data-project-template-autohydrate` is now vestigial (set by the loader but never read) — left in place (harmless; the loader is a runtime file I can't smoke-test).
- **Iteration 14:** ~~P3 #12 (CLS) was one of three browser-free candidates; per "performance before content/architecture" it was the priority pick.~~ — **done**, after an honest re-audit that re-scoped the item. The backlog line over-stated the gap: work-card images were already reserved by a fixed `height: 230px`, and featured/project images by `aspect-ratio: 16/9` — so the literal "card image CLS" didn't exist. The genuine residual gaps were the **brand logo** (no `width`/`height`, no CSS aspect-ratio → small snap) and the **LCP hero `#fr-main-image`** (no `fetchpriority="high"`). Fixed both: logo → `width="119" height="60"` (real intrinsic); hero → `fetchpriority="high"` + `width="1280" height="720"`. Every homepage image now has reserved space; the LCP element is high-priority.
- **Iteration 15:** ~~extend `scripts/prerender-project-pages.js` to also generate the per-page JSON-LD from the config (deduping the four hand-maintained blocks)~~ — **done.** First re-audited the drift framing honestly: the JSON-LD values *deliberately* differ from the config's in-page fields (Division 2's headline, Far Cry 6 & raiden's social image, all four's polished SEO description vs. the in-page hero copy), so they can't be *derived* from existing fields — the real defect was raw JSON hand-typed into HTML across four files with duplicated boilerplate and no validity check. Added a `seo` block to each config (verbatim from the current JSON-LD) and extended the prerender script to generate a `JSON.stringify`-validated `Article` JSON-LD block in the `<head>` (idempotent markers; `url` derived from `CNAME` + filename). Values preserved exactly (4/4); cross-tag consistency (url==canonical, image==og:image, description==meta description) verified; idempotent (byte-identical re-run); `index.html` untouched.
- **Iteration 16:** ~~have the prerender script also sync `<meta name="description">` / `og:description` / `og:image` from `config.seo` (closing the residual meta↔JSON-LD drift surface noted in iteration 15's scope boundary)~~ — **done.** The script now generates the entire `<head>` SEO meta cluster (title/description/canonical/og:*/twitter:*) from `config.seo` + a new `seo.ogDescription` field (the shorter social-card copy), with idempotent `project-meta` markers and per-tag hand-maintained-strip regexes (assert ≤ 1 per tag). Values preserved exactly 40/40 (unescaped); canonical/og:url/jsonld.url and og:image/twitter:image/jsonld.image all agree; idempotent (byte-identical re-run); `index.html` untouched. The key implementation lesson: the meta strip's trailing consumer must be `[ \t]*\r?\n` not `\s*`, or it eats the adjacent JSON-LD block's leading indent and leaks a duplicate JSON-LD block on re-runs.
- **Iteration 17:** ~~P3 #13 (featured carousel `preloadImage` contention) — chosen over P4 #17 (blocked: no email in repo to build a contact CTA from) and P4 #16 (mostly documentation — the About skill-tags and the taxonomy are different dimensions) per "performance before content."~~ — **done.** `preloadPageAssets()` now warms only neighbor **covers** (was cover + all thumbs + all HighRes fulls per neighbor ≈ 18 speculative fetches); also removed two redundant same-URL preloads in `renderPage()` (the visible `<img>` `.src` already fetched the cover and each thumb). Net: the carousel speculatively fetches 2 neighbor covers instead of up to ~18 images; navigation still instant (covers warm), thumbs/fulls fetch on demand. Zero functional change; verified by `node --check` + serve+grep. The key insight justifying the browser-free call: removing speculative/redundant image requests provably cannot break rendering (every image still loads when displayed), so the "ideally wants a browser smoke-test" caveat from iteration 16 doesn't apply to a pure preload-trimming change.
- **Iteration 18:** ~~candidates were P4 #16 / a content pass / P4 #15~~ — **done.** Took the content axis: the objective's CONTENT REVIEW pillar had no dedicated artifact (only this CODE-REVIEW.md existed). Fixed two shipping content defects — the d-walker `ago(2015)` missing-space typo (the page's only body paragraph) and the division-2 placeholder `"Header"` `h3` (the file's only non-descriptive heading, removed; banner preserved) — re-ran the prerender so the `<noscript>` stayed in sync with the edited configs. Opened [`CONTENT-REVIEW.md`](./CONTENT-REVIEW.md) with the full per-project assessment + the P4 #15 naming table/policy + R2-side filename typos. **Resolved P4 #16** as a documented non-defect (skills and taxonomy are different dimensions; only Python/Houdini overlap; no contradiction).
- **Iteration 19:** ~~a fresh-audit pass (the explicit backlog was depleted to owner/browser-gated items)~~ — **done.** Fixed two static-HTML defects, no JS change: removed a stray `<!-- Retry deployment after transient GitHub Pages 502 annotation -->` from `index.html`'s `<head>` (leftover from the `23aa70d` "Trigger Pages redeploy" workaround — same cruft class as the iter-1 junk file / iter-3 test artifact); and tightened **P2 #10** — the six resume `<time>` date-range elements were non-conforming (no `datetime`, range text), now carry a `datetime` start-month each (visible text unchanged; all 7 `<time>` conforming). Also filed an observed/browser-gated finding: the carousel thumbnail `<button>`s nest inside `<a class="fr-card">` (DOM-valid via `appendChild`, but overlapping interactive semantics) — deferred to a browser-checked structural pass.
- **Iteration 20:** ~~fresh-audit pass over site-completeness / IA (explicit backlog was depleted to owner/browser-gated items)~~ — **done.** Added the two missing site-completeness artifacts no prior iteration had touched: a **favicon** (the site had none — browsers auto-requested `/favicon.ico` and 404'd on every page load; generated a multi-res `favicon.ico` at root + `images/apple-touch-icon.png` from the brand logo via Pillow, wired `<link rel="icon">` + `apple-touch-icon` into all 5 pages, placed *before* the viewport `<meta>` so the prerender's meta-cluster re-insertion can't displace them — verified idempotent) and a **custom `404.html`** (GitHub Pages was serving its generic off-brand 404; added a self-contained, on-brand page with a `noindex` + "Return to homepage" CTA). Both static assets, fully validated without a browser.
- **Iteration 21:** ~~fresh-audit pass over motion accessibility (explicit backlog was depleted to owner/browser-gated items)~~ — **done.** The site honored **no** `prefers-reduced-motion` preference despite 21 CSS transitions and 3 JS smooth-scrolls. Fixed **both** halves: added a `prefersReducedMotion()` helper to `shared.js` (conservative `false` fallback) and gated all 3 `scrollIntoView` calls on it (CSS can't reach programmatic scrolls), **and** added a site-wide `@media (prefers-reduced-motion: reduce)` block to `style.css` (universal selector, `0.01ms` idiom — removes motion, preserves final states; verified no `transitionend` reliance). New backlog item **P2 #25** filed+resolved. The same audit noted a separate minor gap — **no `<meta name="theme-color">`** (mobile browser-chrome theming) — left as a clean future one-line-per-page candidate.
- **Iteration 22:** ~~fresh-audit pass over browser-chrome theming + dark-mode signaling (the `<meta name="theme-color">` candidate noted in iteration 21)~~ — **done.** The site shipped **no** `<meta name="theme-color">` and **no** `<meta name="color-scheme">` on any page despite being unconditionally dark (no `prefers-color-scheme` media query). Added both to all 6 pages (index + 4 project + 404), plus a canonical `color-scheme: dark` in `style.css` `:root` and `404.html`'s inline `:root` (MDN "declare in both" guidance). `theme-color="#0f111a"` (= `--bg`) colors the mobile browser chrome; `color-scheme: dark` is the substantive half — it makes the browser paint a dark pre-CSS-load canvas (no white flash) and theme scrollbars/form-controls dark. Placed the project-page metas **before** the viewport `<meta>` (the iteration-20 idempotent-safe zone): verified by re-running the prerender — all 4 project pages byte-identical (md5 before==after), `git diff` = +2/page with zero drift in the generated clusters. Fully validated without a browser (serve+grep: 6/6 each meta; server stopped).
- **Iteration 23:** ~~deeper runtime/correctness review of the carousel interaction code (the static-`<head>`/site-completeness surfaces are exhausted; the iteration-22 learning said to pivot here)~~ — **done.** Found and fixed a **keyboard-accessibility bug**: the featured-carousel thumbnail `<button>`s carried a `keydown` handler that called `preventDefault()` on Enter/Space, which cancels a native button's synthetic `click` (the default action of Enter/Space) — so the thumbnail's `click` handler (preview swap + `window.location.href` navigate) never fired for keyboard users. The carousel's primary action (go to the project) was **unreachable by keyboard**; invisible in a mouse-only click-through. Internal proof it was a defect: the loader's own `.project-thumb` gallery uses a click-only pattern (no Enter/Space handler) and keyboard-activates fine — the carousel was the only place with the extra handler, which can only suppress the native click. Removed the redundant handler (native Enter/Space → click → navigate now restored); rest of the carousel (focus/blur preview, arrows, mouse/touch click) untouched. Same provably-safe "restores native semantics" class as iteration 17 (no regression for any input method), validated without a browser (`node --check`, grep that the harmful pattern is 0 and click/focus/arrow paths are intact, serve+grep). See the iteration-23 section above.
- **Iteration 24:** ~~the two iter-23-named deeper-runtime candidates (the `loadVersionTag` GitHub-API failure path; auditing the remaining `preventDefault`/event-ordering sites)~~ — **done.** (1) `loadVersionTag()` now caches the resolved commit SHA in `sessionStorage` (namespaced key `maxlyj:version-tag`), collapsing per-session GitHub-API calls from **5 → 1** (homepage module-load + each of the 4 project pages = 5 calls against the shared 60-req/hr-per-IP unauthenticated limit); sessionStorage (not localStorage) is tab-scoped so a post-deploy session always fetches the fresh SHA, and every storage access is wrapped in `try/catch` with a conservative fetch fallback (same pattern as `prefersReducedMotion`) so unavailable storage degrades to the original behavior. (2) Completed the `preventDefault` audit: every site is now legitimate **except** the carousel prev/next nav buttons, which carried a redundant `preventDefault()` + `stopPropagation()` block that was a pure no-op (`.fr-nav` is a `<button type=button>` with no default action, and a sibling of the card `<a>` with no ancestor click handler) — collapsed to a single `movePage` click listener per button. Both are provably-safe logic fixes (no structural/DOM change), validated without a browser (`node --check`, serve+grep: cache markers + 0 `stopPropagation()` calls + only the 3 legitimate `preventDefault` sites remain). See the iteration-24 section above.
- **Iteration 25 (next) — candidates, all owner-driven or browser-gated:** the highest-content-ROI items are owner-authored and noted in [`CONTENT-REVIEW.md`](./CONTENT-REVIEW.md#content-backlog-prioritized) — (1) deepen the d-walker & raiden case studies (they're below the Far Cry 6 / Division 2 bar; *no fabrication*), and (2) rename raiden's generic `Initiative`/`Research Pipeline`/`Research Result` scaffold headings. P4 #15 (apply the documented naming policy to Division 2's taxonomy card title) is a one-line config change once the owner confirms. P5 #19 *part 2* (sidebar/gallery dedup) remains the maintainability item but is still gated on a browser smoke-test (the two sidebar copies genuinely diverge — homepage does in-page hash smooth-scroll + `history.replaceState`; the loader copy only open/close + mobile auto-close). Minor leftover from iteration 13: the vestigial `data-project-template-autohydrate` attribute could be removed in a future browser-checked pass; the iteration-19 nested-interactive carousel finding (`a > button`) likewise wants a browser-checked structural fix (now the *only* known carousel a11y issue — iteration 23 closed the keyboard-activation half). The deeper-runtime-review angle that paid off in iterations 23–24 is **now largely mined out**: the `preventDefault`/event-ordering audit is complete (iter 24) and the version-tag external-API path is hardened (iter 24). The runtime fetch/error paths are also already well-defended on audit — `loadTaxonomyManifest` (home.js) catches its fetch and returns `null` (which `buildFeaturedPages` handles per iteration 2), `loadFeaturedProjectConfig` catches and returns `null` + `console.warn`, and the loader's config/template fetches render the visible error state from iteration 3 — so there is no obvious unhandled-rejection defect left there. Remaining browser-verifiable runtime candidates are therefore genuinely thin (a full click-through + DevTools audit may still surface event-ordering or focus-management edge cases not visible from static reading); otherwise await browser access for the structural items. P4 #17 (contact/email CTA) stays **blocked** until the author supplies a real contact address. P1 #24 (d-walker/raiden real thumbnail assets) stays **blocked** on R2 uploads. *Note:* the static-`<head>`/site-completeness surfaces (favicon, 404, robots, sitemap, theme-color, color-scheme, lang, charset, viewport, reduced-motion) are all implemented — remaining work is genuinely owner-gated (content/assets/contact) or browser-gated (runtime structural changes), so future fresh-audit passes should target the remaining runtime fetch/error-degradation paths or await browser access.

## How to validate changes locally
No build system. Serve from repo root and click through:
```bash
python3 -m http.server 8000   # then open http://localhost:8000/
```
- Smoke-test: homepage tag filter, featured carousel arrows + thumbs, sidebar nav + Escape-to-close, each project page loads content, image-compare slider drags.
- `node --check home.js && node --check project-instance-loader.js` for JS syntax.
- `python3 -c "import json; json.load(open('data/taxonomy.json'))"` for data validity.
- Run Lighthouse + axe DevTools on `index.html` and a project page to quantify a11y/perf gains.
